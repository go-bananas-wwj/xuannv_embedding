# V5 数据操作细则

## 最新核实状态（2026-09-13 23:27 UTC）

验收仍incomplete，未获准训练。以下为当前快照，早期数据范围继续保留。

| 步骤 | 已核实产物/处理规模 | 尚未完成 |
|---|---|---|
| A0—A3 | 原62,000位置及划分不变；吉林一号12/64包完成校验、解码、目录，139,149完整波段文件、330隔离文件 | 其余52包与后续全量处理 |
| B1 | S1/S2的48个月包完成完整性检查；Landsat43像素网格修复、6测试与9实际样例通过，24月v2重跑中 | 完整Landsat重跑；基础物理合同仍缺证 |
| B2 | 高分177,505场景QA完成；吉林一号12包22,387场景、84,386分支完成，1,498场景QA缺失 | 全64包和独立云质量复核 |
| B3 | 高分原始组内177,505完成；高分QA审核110,848/177,505；吉林一号10包QA审核完成，12包3,968/20,893；候选窗口标定及重跑完成 | 独立真实候选验证、QA审核余量、跨分辨率和绝对配准 |
| B4 | 静态源对照与DEM依赖修正锁定；OSM43,936/62,000，原标签对账0失败 | 完整OSM、边界变化处理与最终标签索引 |
| B5 | 吉林一号10包55,977训练分支统计及完整缓存复核完成；12包3,328/67,239；高分121,088/284,230 | 当前统计余量、基础影像、后续吉林目录及最终训练视图统计 |
| B6—C1 | 分项核心、诊断证据及图例留存 | 最终全国索引、各split100位置加载、完整96组材料和验收报告 |

原角落候选标定448例中442可靠、0个错误自信，6例不确定；168负控制全拒绝。
这不是实际跨波段合格率。新Landsat重跑首2包逐文件解码中，未计整包完成；
旧检查停止时65包有结果，其中48通过、17个Landsat结构失败，旧记录保留。
详细参数、实际分项结果、验证及证据见文末。

## 路径与执行合同

部署时显式指定 `--source-root`（吉林一号目录）、`--dataset-root`（新质量版本）、
`--report-root`、`--base-root`（旧全国数据集）。绝不覆盖旧版本。
外部根目录绑定和实际绝对路径保存在数据盘运行记录，不提交私有路径。
统一CLI入口为 `xuannv data prepare-v5`；支持的阶段以实际 `--help` 为准，
未实现阶段必须拒绝执行，不能生成成功标记。

来源固定为 ModelScope `ptyzjr/Jilin1_Aligned_Train`。仅此来源允许远程像素；
其他源本地优先。认证从进程环境读取，禁止打印或持久化Token、请求认证头及签名URL。
预留约600GB空间，涵盖约208GB原包、210GB原生影像及质量产物；监测实际空间。

## A0—A3：落盘与索引

1. **source-lock**：读取发布revision、ARCHIVES.tsv、ARCHIVE_INDEX.tsv、SHA256SUMS；
   交叉核对包名、字节数、校验值。锁定全国registry内容与split计数、旧质量来源。
   输出 `manifests/source.lock.json`、`locks/input.lock.json`。变更已有锁必须拒绝。
2. **download**：首批两个包，最多并发2；`.partial`续传严格校验Content-Range起点、
   总大小；无有效续传重新下载。网络失败最多重试3次，认证错误不重试。
   大小和SHA256全部匹配才原子改名。输出 `manifests/download_status.parquet`。
3. **extract**：包先校验；拒绝路径越界、软/硬链接、设备及重复非等价成员。
   临时目录完成后发布，按包记录完成标记，崩溃可恢复但不能暴露不完整包为成功。
   TIFF数对照manifest；文件流式SHA256，逐块解码验证。
   输出 `raw_file_inventory.parquet`、`archive_integrity.json`、`rejected_files.parquet`。
4. **catalog**：读取实际CRS/仿射变换/1280m bounds，与全国网格唯一匹配；目录仅辅助。
   原采集时间和精度保留，不能推断不存在的时区或使用下载时间。同景不同分辨率
   共享scene_group_id；继承原split，全部年份登记、2020/2021另建视图。
   输出 `observations/highres/jilin1/{files,scene_groups}.parquet`、网格审核和覆盖表。
   不完整组按分支标记；同标识不同内容隔离，等价重复去重并保留来源。

## B1：辐射合同与统一读取

接口返回原生数组、逐波段有效掩膜、变换、CRS及元数据；QA、统计、加载共用。
先read_masks/NoData/非有限值，再逐波段scale/offset，最后QA和标准化。
零值不默认缺失，不统一裁剪反射率至0—1，不重复大气校正。
JL抽样合同为int16、NoData=-28672、scale=.0001、offset=0，逐文件验证。
5m按描述取B1—B6；B0独立；10m只取B7—B12；20m只取B13—B19；
缺失标识不得按通道位置猜测。记录JL1GP01/02及实际波长变体。
S1核查单位及VH/VV，不重复RTC；S2核查处理基线与偏移；Landsat核查RGB及缩放。
未知合同隔离。输出product_contracts、radiometry_audit、band_contract_failures。
测试包括有意义的零/负值、负NoData、每波段不同缩放、打乱波段及缺失分支。

## B2：质量掩膜

JL以B5/B4/B6（红/绿/近红外）调用冻结OmniCloudMask；GF以MS红/绿/近红外，
不得单独将PAN/B0送入多光谱模型。锁定模型权重SHA256、版本及预处理。
保留classes 0晴空/1厚云/2薄云/3云影；仅云/影膨胀约30m（ceil(30/gsd)）。
NoData单独保存；最终掩膜为数据有效与非云缓冲交集。传递掩膜使用真实CRS和
transform，细到粗保守汇聚，不能仅resize或将投影写为固定EPSG。
保留缓冲前后掩膜，即便晴空率<60%也不清零；单独保存strict_scene_qualified。
输出每产品classes.zarr、valid_masks.zarr、observation_quality.parquet。
旧sidecar仅在来源、合同及掩膜语义可验证时复用；仅有已清零掩膜不能恢复为新版本。
测试全云、全空、局部云、边界、不同分辨率传递及严格筛选不修改原掩膜。

## B3：配准

同年清晰基础参考仅供检查；多个有效窗口做边缘/平移匹配，记录偏移、置信度、
有效窗口数及一致性。算法参数用固定样例和已知人工平移测试冻结；未经标定不得
声明亚像元通过。可靠匹配残差≤5m可进入像元监督；超限或不确定隔离并分别统计。
保留原始影像，不自动扭曲修复。输出quality/alignment、alignment_summary和样例。

## B4—B7：可读取的训练数据

1. **targets**：检查标签网格、类别和年度；2020样本不取2021标签。DEM声明静态；
   OSM保留正例/可靠负例/未知及置信度。只建引用manifest，不复制旧大数据。
2. **statistics**：仅train有效像元，按产品及必要变体，流式均值/方差，分位数
   标明精确或抽样方法。每观测计一次，不随年度四季度复用重复计数；报告区域/年份
   贡献。空通道、异常方差失败；保存来源与质量合同指纹，用小数组对照直接计算。
3. **sample-index**：键patch/year/quarter；基础只取当季3月，高分取同年全年。
   保存全部候选；默认每年GF4组、JL4组，先每季度最高有效面积一组，再补剩余。
   按有效面积降序、日期及场景ID稳定排序；训练/验证/test继承原split。
   输出quarter_samples、annual_highres_candidates、annual_highres_default_selection。
4. **verify**：各split100位置、两年四季，不调用嵌入模型；检查原生shape、缩放、
   掩膜、缺月/仅雷达/无高分/部分分支/全云；原始与缓存一致，重复运行有效跳过、
   源指纹改变拒用旧产物。输出loader_verification、reproducibility_checks和dataset.lock。

## C1：验收产物与停止条件

`acceptance_report.md`：文件对账、按年/区域/split覆盖、云/配准前后保留量、
GF/JL覆盖四组合、异常影响、逐项检查及源/代码/数据版本。
`visual_review/index.html`及samples.csv：96组，基础光学/GF/JL各32，覆盖晴空、
多云、薄云、云影、边界和配准异常；不足如实说明，原图/分类/掩膜/网格对照。
`locks/acceptance.json`：未通过硬检查为incomplete；全部检查通过才ready_for_review，
自动程序无权写accepted。实际用户验收绑定数据锁hash，质量变化使旧验收失效。
发布验收材料后停止；模型计划保留在主计划，不在本阶段执行。

## 每步记录与提交

状态须含step、input_fingerprint、parameters、outputs、counts、excluded_reasons、
checks、code_commit、status、started_at、finished_at。运行中/失败不能改为成功。
阶段输出临时写入后发布；进程互斥，禁止两个进程同时修改同一运行。
每个行为改动先失败测试后实现，执行针对性pytest、Black/Ruff及仓库/发行门禁，
每个改进单独提交并push至wwj。外部完整清单不提交Git。

## 实施记录：2026-09-13（进行中，未提交最终验收）

本节是代码交付和实际执行快照，**不是全量数据处理完成声明**。数据盘中的
`stages/*.json`、`progress.json`、各产品进度文件记录实际部署路径、参数、数量和时间。
完整数据盘路径绑定在 `locks/input.lock.json` 及阶段参数中；Git仅保存脱敏目录合同。
原有工作区中的模型、训练和配置改动未纳入本次数据升级提交。

### 当前输入基线

- 全国网格已锁定：62,000位置；train49,600、val6,200、test6,200，沿用原划分。
- 吉林一号固定revision：`f7dcc7ade6e2c65ffbcdc6a040a1823458594e78`。
  64包，742,115张TIFF，61,927个发布位置标识；压缩208,290,405,248字节，
  发布清单中的原文件210,125,547,669字节。发布位置数不等于2020/2021合格覆盖数。
- 基础影像72个月度ZIP，现有文件名清单含S2 1,478,047、S1 1,467,180、
  Landsat 1,480,709张TIFF；这是包内清点数，尚不代表全部像素解码通过。
- 高分使用未被60%整景门槛过滤的177,505个场景组作为质量检查输入；
  旧104,808组过滤视图不能替代完整候选目录。

### 每一步的实际执行及剩余工作

| 步骤 | 已实现/已执行 | 实际输出（相对对应根目录） | 检查及未完成项 | GitHub代码提交 |
|---|---|---|---|---|
| A0 | 已固定JL发布版本、四份来源清单、全国网格及划分 | source根`manifests/source.lock.json`；data根`registry/`、`locks/input.lock.json` | 网格62,000，划分不变；原数据保留 | `e832195`、`07c2e46` |
| A1 | 双包并发下载已运行；包内8个16MiB区间并发，严格校验Range，整包最终SHA不变 | source根`packages/`、`manifests/download_status.parquet` | 首批2/64包已通过大小和SHA256；合计6.534GB；64包全量未完成 | `07c2e46`、`dd7b855` |
| A2 | 安全解压、逐块TIFF解码已实现；随首批整包校验通过后执行 | report根`integrity_shards/`及`archive_integrity.json` | 首批2包23,261张TIFF全部解码通过；不足完整包不建立成功标记 | `07c2e46`、`774e98c` |
| A2基础源补充 | 72个本地ZIP的全量CRC、SHA、TIFF解码及全国网格检查已启动 | report根`dense_integrity_shards/`、`dense_integrity_summary.json` | 每包流式inventory和missing.parquet；物理合同未知状态独立保留；全量未完成 | `8f69d87`、`098cee7` |
| A3 | JL基于CRS/bounds唯一匹配、观测分组、冲突隔离已实现；后续任务会按已校验分包自动执行 | data根`observations/highres/jilin1/`；report根`grid_match_report.json` | 首批2包23,209文件通过，52文件缺波段隔离；1,930位置/6,745场景；全量未完成 | `acfff17`、`6f1784c` |
| B1 | 原生读取、明确波段选择、掩膜后缩放的核心已实现；基础72包216张完成诊断抽样 | report根`radiometry_audit.parquet`、`band_contract_failures.parquet` | S1次序说明冲突，LS RGB/B2-B4冲突，S2处理基线/缩放缺证；未知合同不通过 | `b3a15de`、`2826df0` |
| B2高分 | 冻结OmniCloudMask真实129/256组试运行退出正常；完整177,505组冻结推理已完成 | data根`quality/cloud/gaofen/{classes,valid_masks}.zarr`、`observation_quality.parquet` | 有效像素不再因整景<60%清零；30m缓冲按真实地理网格传至PAN；全量0拒绝，视觉与配准审核仍未完成 | `51c674d`、`efb9694`、`1946713`、`e753305` |
| B2吉林一号 | B5/B4/B6云输入、同景B0/10m/20m掩膜传递已实现，模拟端到端测试通过 | data根拟生成`quality/cloud/jilin1/` | 真实32场景/125分支试运行完成，31独立位置；全量未运行；全部包和目录齐备后自动推理 | `efb9694`、`6f1784c` |
| B3 | 多窗口偏移、一致性和纹理不足判断核心已实现，已知平移测试通过 | 拟生成`quality/alignment/`及报告 | 尚缺真实固定样例标定、可靠同年参考及全量检查，不能声明配准合格 | `807aa64` |
| B4目录 | 已核对306个标签数组的形状、年份声明及全国位置顺序 | data根`targets/manifest.parquet`；report根`target_audit.json` | 当前manifest明确标为仍需数值和来源审核，不是批准的监督标签索引 | `2826df0` |
| B4数值 | 306数组的逐块数值检查全量完成，0失败，检查类别/非有限值/OSM状态/负例冲突 | report根`target_value_shards/`、`target_value_progress.json` | 重跑读取并哈希实际像素，像素变化不得复用旧结果；年度交叉审核单独执行 | `76e7bdb` |
| B4来源 | 10个源文件检查完成，失败0；8份静态分片来源一致 | report根`target_source_audit.json` | 3个OSM索引匹配原SHA；7个静态源只匹配原size/mtime并新增当前SHA，不能声称历史SHA验证 | `798e741` |
| B5 | 有效像元流式均值/方差、确定性有界分位数抽样核心已实现，直接计算对照通过 | 训练集正式统计**尚未生成** | 尚需连接全部合格观测、地区/年份贡献和源指纹；不是已完成的statistics阶段 | `7512427` |
| B6 | 季度不越界、同年度先验复用、每类最多4场景、全部无高分区域保留的索引核心已测试 | 全国季度/年度候选与默认选择表**尚未生成** | 等待质量、配准及物理合同；核心函数不是已物化的sample-index阶段 | `7512427` |
| B7 | 已有核心数据单测和原生读取检查；NPU推理与训练运行时隔离 | report根`npu_cloud_buffer_regression.json` | 三划分各100位置、两年四季正式加载、缓存对照、dataset.lock**尚未完成** | `1946713`、`e753305` |
| C1 | 已生成32组真实高分独立位置图；进度报告及禁止自动批准机制已执行 | report根`visual_review/index.html`、`samples.csv`、`acceptance_report.md`；data根`locks/acceptance.json` | 基础/JL各32样例尚缺；疑似雪地云误判待核；状态incomplete，训练未授权 | `2826df0`、`82468da`、`774e98c` |

### 实际可用CLI与有限后续处理

下面是当前实现的入口，均使用相同四个显式根目录参数：

```text
xuannv data prepare-v5 --stage source-lock
xuannv data prepare-v5 --stage ingest
xuannv data prepare-v5 --stage catalog
xuannv data prepare-v5 --stage radiometry --dense-root <本地基础影像根>
xuannv data prepare-v5 --stage dense-integrity --dense-root <本地基础影像根>
xuannv data prepare-v5 --stage gaofen-quality --gaofen-source-catalog <未过滤场景表> --model-dir <冻结模型目录>
xuannv data prepare-v5 --stage quality --model-dir <冻结模型目录>
xuannv data prepare-v5 --stage targets
xuannv data prepare-v5 --stage target-values
xuannv data prepare-v5 --stage target-sources
xuannv data prepare-v5 --stage visual-review --quality-root <已完成高分QA批次>
xuannv data prepare-v5 --stage report
xuannv data prepare-v5 --stage followup --model-dir <冻结模型目录>
```

上述为阶段选择说明，执行时须补齐`--source-root`、`--dataset-root`、`--report-root`、
`--base-root`。现已支持alignment-calibration与band-alignment；正式基础参考的跨传感器alignment、
statistics、sample-index、verify仍待集成与实际处理。
这些剩余核心到全量产物的集成继续推进，不能用内部波段检查替代跨传感器配准。

`followup`跟随已经启动的有限数据作业：按新增像素校验包更新catalog，全部64包
catalog完成且高分推理释放设备后执行JL质量处理，每30秒刷新外部进度。
它不训练、不批准数据，失败保留退出码和阶段日志，现有作业结束后停止。
输入版本变化由各阶段锁拒绝；catalog/QA失败不会被改写成完成。

### 运行问题与验证证据

- `open_issues.json`保存每个问题的步骤、影响和状态；缺少基础源导出合同是实际阻塞项。
- 真实NPU试运行曾在退出释放阶段失败；数据CLI隔离Torch/训练运行时后，129和256组
  独立试运行正常结束，前32组分类与置信度逐项一致。失败批次仍保留失败记录。
- 当前32组高分图属于早期检查样例，不满足96组完整验收覆盖要求。
- 完整仓库回归569项通过（1条已有依赖弃用警告）；随后针对真实基础影像的浮点
  仿射误差修复，2项定向回归再次通过。Black、Ruff、仓库门禁和发行包构建通过。
  精确数量及提交绑定见外部`code_verification.json`。
- 每项代码提交已push到远端`wwj`并核对SHA；提交标题和SHA可沿上表直接检索。
  数据报告中的运行提交可早于最新文档提交，具体处理代码同时以文件SHA指纹记录。

基础源全量审核的首轮精确浮点比较曾产生误报，原审核已移入外部diagnostics并标记失效。
修复仅允许1e-9米/像元的数值容差，保持全国网格匹配和仿射方向检查；原影像没有改写。

2026-09-13 12:22 UTC的高分处理快照：28,688个已处理场景中，11,289个场景低于60%
整景门槛但仍有有效像素。使用同一批当前云分类，逐像元策略保留其中76,278,193个
MS像素和1,220,443,002个PAN像素。此为重复观测像素计数，不是独立覆盖面积或模型收益；
尚未经过最终配准和云质量验收。冻结快照来源SHA及计数见外部`gaofen_retention_snapshot.json`。

对已推送`098cee7`的独立Git文件快照另行验证：357项测试通过（1条依赖警告），
Black/Ruff、仓库内容门禁、sdist和wheel构建通过。该验证不包含原工作区未提交文件。

首包吉林一号实际目录（仅1/64包，不能外推全国覆盖）：2020年658位置/1,024场景，
2021年547位置/818场景，2022年913位置/1,482场景，2023年35位置/39场景。
2022/2023仅保留来源目录，不进入2020/2021年度先验。25个隔离分支是实际波段不全，
例如20m文件只有17通道；原始像素可解码不代表通过完整波段合同。

吉林一号真实32场景云推理正常退出，原生各分支125个质量记录已落盘；共31个独立位置。
诊断图`jilin_pilot_preview.png`发现疑似亮建筑/地表被判为云，并有可见彩色边缘现象，
尚未确认原因；不能将冻结模型输出直接当作云真值。该单张诊断图不是正式96组验收材料。

截至首批2包完整核验后，目录累计23,209文件通过、52个不完整波段文件隔离，
1,930个位置/6,745个场景。2020年1,315位置/2,058场景，2021年1,104位置/1,648场景；
这仍只是2/64包的原始合同覆盖，尚非全量QA/配准后的覆盖。后续分包已继续下载。
32场景JL试运行使用首包目录；其`catalog.snapshot.parquet`已保存在pilot目录，
SHA与原质量锁完全一致，后续增量catalog不会使该试运行失去原始输入快照。

### 下载恢复审核与续传修正（2026-09-13 15:30 UTC）

重新核实发现旧下载进程已退出：第3、4包在12:59 UTC失败，不再将其标为正在运行。
旧错误记录只有RuntimeError，不能追溯具体网络原因。两包保留67个已校验16MiB分块，
另有14个分块已写入14MiB。实际公开源的剩余2MiB范围请求返回正确206并完整读取。
复现了旧逻辑在网络中断后丢弃分块内部进度的问题；尚不能断言旧中断的网络根因。

修正为从当前已写字节继续请求，严格核对起点、终点及总大小后才追加；每次运行仍仅
初次请求加最多3次重试。认证/范围错误立即停止该包排队请求。每次错误保存分块范围、
HTTP状态、异常类型、已写字节和尝试次数，绝不记录异常文本、响应正文或签名URL。
事件记录在`.parts/*.events.jsonl`，整包完成后保留为`*.transfer.jsonl`；最终仍以发布
SHA256验证整包，未校验的局部字节不能计作完成文件。源指纹缺失时拒绝复用局部字节。

4项先失败的回归覆盖内部续传、错误范围、认证停止、脱敏诊断与重试上限；修正后全部通过。
当前V5定向测试49项、现有工作树全量测试573项通过（1条既有依赖警告），
Black/Ruff、仓库门禁和发行构建通过。此改动不改变像素质量
合同和已完成的GF云产物；完整数据交付仍未完成。实际恢复PID与结果记录在数据盘。

同次进程核验确认：GF全部177,505场景云推理完成，306项标签数组数值检查完成，均无
运行失败。GF状态仍需视觉审核，标签状态仍需来源/年度合同审核，二者均不是自动验收。

### B4年度OSM交叉审核入口

新增`target-temporal`，读取已完成的目标manifest、306项数值审核中的OSM记录、原始
年度索引SHA审核，以及原始OSM Zarr。逐年逐类把states、confidence、targets和source_bits
一同分块读取；重新计算实际数组SHA，必须与此前数值审核一致。2020/2021、10m/2.5m
所有声明通道必须齐全。只接受已明确无外部证据的基础侧车；外部软标签须另有年份证据。

固定合同来自本地原流水线：来源位1为历史OSM，位2为当前OSM；历史正例须有来源位1、
置信度255及非零覆盖值，未知状态须置信度/监督值均为0。只有当前来源的像素保持未知。
历史边界被保守置未知允许存在；可靠负例overlay继续单独审核，不能混进基础正例合同。
输出`osm_temporal_audit.parquet`、逐组SHA记录、`osm_temporal_progress.json`；
不复制标签、不改写原数组，不用数值范围正确替代年度来源正确。该步骤也不证明原始
OSM栅格化的几何精度、全年地物不变或可靠负例规则正确，相关门禁继续独立保留。

7项先失败回归验证当前来源不能变历史正例、未知值一致性、未注明年代的外部证据、
已改变有效覆盖值的旧SHA失效及CLI路由；当前V5测试56项通过。后续全量运行应覆盖
68个语义组（每年30个10m类、4个2.5m结构类），运行记录分别标示已审核和失败数量。

年度交叉审核提交前检查：现有工作树全量580项测试通过（1条既有依赖警告），
Black/Ruff、仓库门禁、sdist/wheel构建通过。下载恢复提交`8d303e1`已push并核对远端；
恢复后已中断的14个分块均补齐校验，后续字节持续增加，完整包数仍须以发布SHA为准。

### B2固定吉林一号样例的5m/10m云推理对照

问题证据：原生5m试运行存在大面积疑似地表被判云的样例。官方说明5m可用，但通常
10m更合适，周围空间上下文比继续提高分辨率更重要；当前96像素窗口在5m时覆盖480m，
在10m时覆盖960m。这构成待验证假设，不是已证明的误判原因。
来源：[官方分辨率说明](https://omnicloudmask.readthedocs.io/en/latest/resolution.html)、
[官方空间上下文说明](https://omnicloudmask.readthedocs.io/en/latest/spatial-context.html)。

新增`cloud-resolution-audit`，必须显式给出已完成pilot的`--quality-root`和冻结权重。
输入首包32场景的原目录快照（31个独立位置），逐文件SHA、原质量锁、权重SHA都须匹配。
先重跑5m云推理并逐像元对照旧分类和最终掩膜，再对同一R/G/NIR输入以面积平均降至10m；
任何缺失输入像元覆盖到的10m单元保持无效。10m云/影仍缓冲30m，使用真实CRS和仿射
投影回5m网格，与原5m结果对照。RGB显示使用已核实B5/B4/B3，拉伸只影响样例图。

输出位于报告目录`diagnostics/jilin_resolution_5m_10m_v1/`：32组原图、两种分类、两种
最终掩膜和差异图、comparison.parquet、samples.csv、index.html、来源锁及状态。
这不是96组正式验收材料，也不覆盖正式QA。候选数组只有诊断名称，不进入训练索引。

实际运行32/32场景成功，原生重跑与旧分类及掩膜完全一致。5m有效像素352,113；10m
候选投影后1,554,452；新增有效1,202,385、减少46。分母为2,097,152个观测像素，存在
同位置不同时间，不能换算成独立覆盖面积。图00的可见地表条带、图26的局部亮云与
背景纹理值得进一步审核；图13在两配置下仍大范围屏蔽。**没有人工云真值，不能把这些
数值写成准确率增益**。下一步需要固定云/薄云/云影/雪地/亮地物审核区域和跨波段配准
证据，再决定是否发布新的质量版本；原5m质量产物保持可对照。

此对照未改变嵌入模型或运行训练优化器。原生范围、NoData保守传播、统计差异语义及
明确CLI输入有3项回归；V5定向59项通过，实际32场景正常退出。

云分辨率对照提交前检查：现有工作树全量583项通过（1条既有依赖警告），测试进程限制
OMP/OpenBLAS/MKL各1线程。首次全量曾有既有fork测试5秒队列超时；该测试单独复查
通过，未改动用户原有测试或数据加载器，随后全量复查通过。仅终止了本次超时检查
遗留的子进程，其他数据任务未中断。

独立暂存Git快照另有369项首次通过；2项CLI因快照尚无Git HEAD失败，给快照初始化
提交后原样复查通过，共371项。源文件没有为复查作修改。独立快照的Black/Ruff、
仓库门禁及sdist/wheel均通过。运行日志和树SHA保存在外部代码检查记录。

### 2026-09-13 15:58 UTC 执行与GitHub核对

| 改进 | 已推送代码提交 | 实际处理结果 | 状态 |
|---|---|---|---|
| 分块内部续传、脱敏诊断和有界重试 | `8d303e1` | 第3/4包分别88/89个分块完成校验，累计缓存约3.0GB；恢复以来暂无新失败尝试 | 下载活跃，整包通过数仍为2/64 |
| 年度OSM来源/状态/置信度/覆盖值交叉审核 | `53e7ca9` | 已完整扫描22/68语义组，0失败；源与数组SHA参与审核 | 全量运行中 |
| 冻结5m/10m云推理与可视化对照 | `6b7554c` | 32/32真实场景、31独立位置，原生重跑一致，32组图和差异表落盘 | 诊断完成，未决定正式QA变更 |

三项提交均已push至远端wwj并核对SHA。基础完整性当前10/72个月度包通过像素/几何
检查，尚未核实物理合同；高分177,505场景和标签306数组的已完成结果继续保留。
核验时下载、基础全量检查、年度交叉审核、有限后续处理四个进程均确实存活。
冻结云后续使用当时空闲的设备7；设备1—6有其他作业，未对其进行停止或改动。
完整PID、进程身份、来源锁及实际路径记录在外部`goal_iteration_20260913_recovery.json`。

当前交付仍是执行中的数据版本，acceptance为incomplete、training_authorized=false。
剩余步骤不能用单测、这32组诊断图或后台启动记录替代；持续完成A0—C1，最终再提交用户验收。

### 配准核心修正：真实纹理标定暴露的错误合格判定

固定输入为训练划分中16张吉林一号影像（JL1GP01/02各8个位置）和40张高分MS影像
（GF1/1B/1C/1D/6各8个位置）。在同一绿色波段上分别注入(0,0)、(0,0.5)、(0,1.5)、
(2,-1)像素平移，共224例；数值、有效掩膜和注入位移均已锁定。原/新算法使用完全
相同的影像清单SHA，不换样例。两次结果分别保存在intraband的v1/v2目录。

旧64像素窗口的循环相位相关受到平滑背景及裁切边界影响：例如真实纹理背景上的
7.5米注入偏移被估计为1.5米。224例中，吉林一号7例、高分6例的超限偏移被误判合格。
因此此前随机纹理单测通过不足以发布配准质量结果，未用旧算法执行全量合格判定。

新方法在实际有效区域做归一化、Sobel边缘提取，去除缺失像元周围的梯度污染；
每个64×64参考窗口在带16像素真实周边的搜索区域匹配。互相关的计数、均值、方差
只用两端均有效的像元计算，避免补零边缘变成对齐依据；再局部细化亚像素位置。
保留≥70%有效面积、相关≥0.65、至少3窗口、一致性≤0.5像素及残差≤5米门槛，
新增重复/平坦峰检查。参考图、移动图及原始影像均不改写。

修正后上述56张真实纹理全部通过已知平移标定：224例中错误合格判定降为0。
吉林一号最大恢复误差0.025像素，高分为0；这些数值只描述受控同波段注入试验，
**不代表跨波段或跨传感器地理配准已达到同样精度**。真实配对仍须逐项运行并报告
超限/不确定情况。完整前后数量、样例来源及SHA见`alignment_calibration_comparison.json`。

新增平滑背景掩盖偏移、线性坡面缺乏唯一匹配的先失败回归；修正后通过。当前包含
其他已有改动的工作树589项全量测试通过（1条既有依赖警告），CPU线程限制与前轮一致。

### 原生波段配准的完整数据入口

新增`alignment-calibration`与`band-alignment`；显式指定`--sensor-family jilin1|gaofen`、
`--alignment-version`和四个数据根目录。校准与全量处理各自按产品互斥，阶段记录按产品
分文件，不能由一个产品覆盖另一个产品的执行状态。

- 吉林一号输入：`observations/highres/jilin1/files.parquet`中2020/2021的5m MS观测。
  原生6×256×256，以B4为参照检查B1/B2/B3/B5/B6；文件名位置不代替波段标识。
- 高分输入：完整`quality/cloud/gaofen/observation_quality.parquet`所指向的原始MS。
  原生4×160×160，以green为参照检查blue/red/nir，维持明确的stored DN读取合同。
- 标定输入：只从train选择每个卫星变体8个不同位置，固定后不因新增下载而换样本。
  全量开始前要求各变体标定合格，锁定NumPy、SciPy、Rasterio/GDAL版本及算法源码SHA。
- 原始读取：实际文件SHA须与观测目录一致；读取原生值和逐波段有效掩膜，每一对波段
  使用各自掩膜的交集。无纹理、匹配歧义和局部偏移不一致均不能归入passed。
- 输出：`quality/alignment/intraband/<产品>/<版本>/`下的calibration_inputs.parquet、
  calibration.json、inputs/<输入指纹>.parquet、分观测receipts及最终observations.parquet；
  报告目录`intraband_progress_<产品>.json`记录实际处理、通过、超限、不确定、拒绝数量。
- 重复运行：重新核对源文件SHA后才复用；缓存同时绑定观测ID、位置、年份、split、
  传感器、算法及标定锁。标定重跑核实输入不变后复用原锁，不因更新时间戳使全量结果
  无效。像素、元数据或运行库改变时拒用旧结果；不能仅凭文件存在跳过。

部署使用`--alignment-version v5`：前面的v1保留旧算法失败证据，v2保留修正算法的
同批对照，v3/v4记录缓存和运行版本校验的实施过程；最终入口类型与校验合同固定于v5。
这些版本均沿用同一批56个标定观测。中间版本的三个测量源码文件已按记录SHA归档，
不是只留下无法复现的哈希。此处版本号仅指原生波段审核，不是新训练或嵌入模型版本。

实际命令模式（执行时补齐四个根目录）：

```text
xuannv data prepare-v5 --stage alignment-calibration --sensor-family gaofen --alignment-version v5
xuannv data prepare-v5 --stage band-alignment --sensor-family gaofen --alignment-version v5 --workers 2
xuannv data prepare-v5 --stage alignment-calibration --sensor-family jilin1 --alignment-version v5
xuannv data prepare-v5 --stage band-alignment --sensor-family jilin1 --alignment-version v5 --workers 2
```

检查范围是输入锁中全部已入库观测。吉林一号仍未完成64包下载，部分目录的审核完成
不等于全国审核完成；新增源会产生新的输入清单，原有合格观测可经SHA复核复用。
所有结果明确`pixel_fusion_authorized=false`，因为内部波段通过仍不证明与基础参考的
绝对配准。实际跨传感器配准、完整统计/索引/加载及最终验收仍是剩余工作。

上述缓存、源变化、运行库变化、缺失纹理、已知偏移及CLI前置条件由定向回归覆盖；
完整工作树590项测试通过（1条既有依赖警告），补齐类型声明后10项相关回归再通过。
Black/Ruff、构建检查通过。配准核心修正`63f54b1`已push并核对远端SHA。


2026-09-13 16:53 UTC执行核对：`86a5528`已push并验证远端SHA；原生波段审核已实际
处理首个128观测批次。GF输入177,505场景：首批121 passed、7 uncertain；JL输入当前
3包目录中的5,245个2020/2021 MS观测：首批6 passed、29 over_limit、93 uncertain。
这是按观测ID顺序读取的进度，**不能推算全国比例，也不能把光谱差异导致的匹配不确定
当作已证实错位**。原始影像保持不变，后续固定抽样做视觉复核。

新增有限续查机制：每批已校验包先更新目录，然后比较原生波段输入指纹、版本、标定
SHA、算法SHA和最终产物；当前目录未审核时运行band-alignment，已有同产品审核进程
存活时等待。只有完整目录的审核已结束后才进入原有全量云推理。超限/不确定仍保留为
异常状态，审核结束不等于像元级融合合格。失败的同一阶段/输入不会在轮询中无限重试。
续查显式传递`--alignment-version v5 --workers 2`；该父进程仅协调已授权数据阶段。
回归覆盖增量目录顺序、活跃任务避重、版本/源/算法变化、失败停止及不得进入训练。
12项相关测试、全仓Black/Ruff与内容门禁通过，构建通过后提交此改动。


### 固定原生波段诊断图（B1/B3辅助证据）

入口`band-review --sensor-family <产品> --alignment-version v5`使用四个根目录。
输入为已校准算法的实际分观测receipt及当前目录，逐项复核receipt SHA、文件SHA、
年份/位置/split和标定锁。首次从当前已完成receipt按状态、卫星分组轮换抽取12个
不同位置，并固定samples.json；新增下载不会悄悄替换样例，数量不足如实报告。

产物位于报告目录`diagnostics/native_band/<产品>/v5/`：index.html、samples.json、
samples.csv、逐样例PNG及summary.json。图中按波段显示原始灰度、红绿叠加、匹配窗口；
仅对具有可靠估计的配对预览平移，无法可靠判断时不生成猜测位移。RGB和灰度只做显示
拉伸，读数和原文件均不改写。红绿差异也可来自光谱差异，不能仅凭颜色直接判定错位。

这是B1/B3固定诊断证据，不占用C1规定96组带正式云质量与网格叠加样例的名额；
缺少跨传感器参考时，所有图例继续明确不授权像元融合。确定性选择、缺失像元隔离、
源文件无修改、样例固定及源变化拒用、CLI合同均有回归。当前已通过16项相关检查。
增量续查`a1cec3d`已push并核对远端SHA，新协调进程已识别两个现存波段审核作业。


17:02 UTC实际产物：GF和JL各12个独立位置图例已生成。GF按状态为3超限/5通过/4不确定，
JL为4/4/4；这些为刻意平衡的诊断样例，不是发生率。已目视查看JL前两组和GF第2组：
JL部分地形、道路状边缘的红绿分离在估计平移预览后减轻，支持继续排查波段偏移；GF
该例大面积亮云状区域覆盖匹配窗口，不能据此确认地面偏移。解释及对应图片/receipt
SHA写入`diagnostics/native_band/visual_findings.json`，明确为助手诊断而非人工真值或
用户验收。尚未自动校正任何原始影像，也不把这24组替代C1正式96组。

完整工作树597项测试通过（1条既有依赖警告），Black/Ruff、仓库门禁及sdist/wheel构建
通过。上述实际CLI生成成功；图片和逐观测来源清单保留在数据盘。


### B4年度标签从锁定源重建核验

新增`target-geometry --target-family worldcover|clcd|nightlights`，使用四个既有根目录。
`--max-patches 32`明确生成pilot_32诊断；不传该参数才核验完整62,000位置。不同产品
互斥锁分开，试验和全量产物目录分开，不能把首批结果覆盖成全量状态。

输入为冻结registry、targets/manifest.parquet、已完成target_source_audit.json指向的
原始ZIP及target_value_audit.parquet。每年度开始重新计算实际源ZIP SHA并与锁比较；
WorldCover/CLCD/夜光严格选择对应年TIFF成员，排除其他年及不同夜光派生产品。
读取真实GeoTIFF CRS/仿射/范围/NoData/掩膜/比例，文件名只用于产品与年份识别。

按每个1280米网格重建128×128标签：类别最近邻，夜光双线性；插值读取保留邻域。
WorldCover/CLCD相邻有效类别变化的两侧像元保持未知；NoData不会参与插值或成为类别。
类别输出逐像元精确比较，连续值容差atol=1e-5/rtol=1e-6，掩膜另作精确比较；非法
类别、冲突重叠瓦片和无效处非零值单独报错。重建只在内存中进行，不复制标签大体积
存储，也不覆盖原始影像或标签。

每32位置写chunk receipt，绑定源索引/完整SHA、代码、运行库、registry、manifest及
本次实际读取的标签/掩膜像元SHA。重复运行仅复用全部指纹一致的比较结果；全量结束
还要求实际累计标签/掩膜SHA与已完成数值审核一致。输入源在运行中变化则拒绝发布。
产物为新数据目录`quality/targets/geometry/<产品>/full|pilot_32/`中的源成员清单、
chunks及observations.parquet；报告为`target_geometry_<产品>_<范围>.json`，包含
处理/失败/复用数量和限制。该核验确认当前锁定年度源到标签的一致性，不能补造历史
缺失SHA，也不能代替产品本身的独立精度评测。DEM/坡度及OSM几何审核另行处理。

17:11 UTC实际小批次：32位置（29 train、1 val、2 test），两年、三个产品共192项；
源重建与现有标签的有效掩膜差异0、像元差异0、最大绝对误差0。试验保存在各产品的
pilot_32，未写作全国已通过。回归覆盖错年、错网格、掩膜变化、按像元比较、完整读取
缓存复用、实际标签变化拒用和CLI分阶段合同；17项相关检查通过。

该标签重建实现完成后的完整工作树603项测试通过（1条既有依赖警告），全仓
Black/Ruff、仓库门禁及sdist/wheel构建通过。三个产品的实际pilot命令均退出成功。


### 17:17 UTC代码与实际执行对账

`d32b11a`已push并核对远端SHA；从该提交单独导出的干净副本通过391项测试
（1条既有依赖警告）、Black/Ruff、仓库门禁及构建。它未包含原工作树未提交文件，
与完整工作树603项测试分别记录，日志和SHA归档在报告目录code_checks/d32b11a。

三个年度标签全量作业已经启动并有持续输出，每产品62,000位置×两年=124,000项。
- clcd：已比较28,864/124,000项，当前失败0；仍在运行。
- worldcover：已比较35,680/124,000项，当前失败0；仍在运行。
- nightlights：已比较38,784/124,000项，当前失败0；仍在运行。

当前吉林一号完整4/64包：目录46,381合格文件、106隔离分支、3,861个位置；基础ZIP
完整性14/72，年度OSM交叉检查66/68。内部波段审核当前GF3,840/177,505，JL
3,072/5,245（此次输入锁为3包目录，4包新增观测随后续查），均非全国最终结论。
下载、完整性、年度审核、两个波段审核、三个标签重建作业均按实际进程身份核实存活。

重新读取基础源公开仓库的固定README（revision be1e87665e0d02e9e199c9fa4ab3c398541471e8），
实际SHA与发布树一致；13项完整目录无导出脚本，S1顺序及Landsat对应说明的既有矛盾
未消除，S2缩放/偏移证据仍缺。此次仅读取公开文字元数据，没有新增基础遥感影像下载。
机器证据位于diagnostics/dense_remote_metadata；未知物理合同仍不进入训练标准化。

模型改造、模型训练、蒸馏及正式评测均未启动；最终数据状态仍为incomplete。


### DEM及坡度源重建入口

新增`dem-geometry`，四个既有根目录，`--max-patches 32`是明确试验；不传限制才处理
62,000位置的高程与坡度共124,000项。两段原始ZIP均重新计算SHA并对照已锁定来源，
校验分段顺序和ZIP成员的压缩/加密合同。实际源为1,982个未压缩TIFF成员。

使用[GDAL虚拟文件系统](https://gdal.org/en/stable/user/virtual_file_systems.html#vsisparse-sparse-files)
按每个成员的字节范围建立只读描述文件，即使成员横跨两个物理分段也直接读取；
不另拼接73GB档案、不把每个整幅TIFF复制进内存。读取真实CRS、范围和像元掩膜，
按全国网格双线性重建高程。坡度按既有10米差分/缺失处中位数填充合同重建后比较，
同时独立统计“有效坡度的求导邻域含无效高程”的像元，避免只因复现旧行为便认定
填充值有真实观测支持。该检查不自动改写原始DEM或坡度标签。

产物为`quality/targets/geometry/dem/full|pilot_32/`的源索引、逐成员只读描述、分块
receipt、observations.parquet；报告`target_geometry_dem_<范围>.json`分别记录
重建差异和缺失邻域支持情况。全量累计标签/掩膜SHA须与已完成数值检查一致，
原始分段或标签变化均拒用缓存。全无效、负高程、已知斜面、分段边界、实际像元变化
及CLI合同由回归覆盖。

实际首32位置的64项比较通过，最大差异0，未发现缺失高程邻域造成的坡度支持问题；
这仍是pilot，完整数据结果另行记录。源原始文件及标签均保持不变。


### WorldCover接缝核验顺序修正（年度审核v2）

全量WorldCover v1核验完成124,000项，其中5项失败，均为2021年相邻原始瓦片
在重投影接缝上对同一目标像元给出不同类别。实际源瓦片地理边界相邻，不是大范围
原始地理重叠；每个位置仅1个冲突像元，共5个。逐项读取原标签确认这5个像元原本
均为valid=false、value=0。原检查在类别边界掩膜之前拒绝整个位置，顺序与统一
有效像元读取合同不一致。原v1完整报告、输入锁和失败记录保留。

新v2先按原合同形成类别边界未知掩膜，再检查仍有效的像元是否存在源冲突；仍有效
的任何冲突继续报错，不设容忍数量。被原掩膜排除的冲突单独计数，既不变成有效类别，
也不把整片位置误判失败。实际标签值、类别边界规则和逐像元比较标准均未改变。
固定原5例重跑后图值差异0、掩膜差异0，各记录1个已排除源冲突；证据保存在
`diagnostics/worldcover_tile_conflicts/`，含原tile范围、像元坐标、原类别、原报告SHA
和修正后结果。有效冲突仍失败的回归与版本目录隔离回归均通过。

实际入口`target-geometry --target-family worldcover --target-audit-version v2`；输出
新数据目录`quality/targets/geometry/worldcover/v2/full/`，报告
`target_geometry_worldcover_v2_full.json`。未传版本仍使用v1合同，不覆盖原失败产物。
需要v2全量124,000项完成且通过后，才更新这个产品的审核状态。

DEM独立提交`bc3909d`已push并核对远端SHA；仅包含该提交待发布内容的独立副本
396项测试通过（1条既有依赖警告），格式、内容门禁和构建通过。年度OSM交叉检查
68/68已完成且无矛盾；它不代替尚未完成的OSM几何与可靠负例来源审核。

修正后的完整工作树609项测试通过（1条既有依赖警告），Black/Ruff、仓库门禁
和sdist/wheel构建通过。原有效区域内的冲突检查仍严格失败，未降低阈值。


### 已存在输入锁时继续核对实际网格

回归发现旧入口在input.lock.json存在且基础来源未变时直接返回，未再次比较新数据
目录中的实际网格。只改动新目录的split或删掉该副本，会绕过该处检查。修正后每次
阶段启动都要求目标网格存在，且完整SHA与锁定基础网格一致；不自动补写或覆盖差异。
新增先失败回归覆盖目标split被换、网格丢失及正常复用，36项相关检查通过，格式、
仓库门禁和构建通过。该保护不改变已有数据样本或划分。

17:42 UTC实际核对：基础网格、V5副本及input.lock三方SHA完全一致，62,000位置及
划分未修改；机器结果为registry_lock_verification.json。DEM全量已比较2,432/
124,000项，无重建差异/缺失邻域问题；WorldCover v2已比较12,192/124,000项，
当前失败0。两项均未完成，不能据此授予最终数据验收状态。


### 可靠负例的实际证据规则核验

新增统一入口`target-negative-rules`，输入已完成年度OSM交叉检查、标签数值SHA、
同年WorldCover、静态坡度及四类已有可靠负例。按原登记规则重建building、road_all、
water_area、waterway负例掩膜，逐像元比较状态与置信度。正例优先，普通未知区域
继续未知；陡坡只可支持既定water_area规则，不能推导waterway负例。

处理参数取已锁定元数据：腐蚀2像素、陡坡20度、负例置信度224。分块腐蚀不跨位置，
仅使用有效来源像元。每块绑定实际输入及输出像素SHA、年份、来源元数据、算法和
运行库版本；全量累计SHA须匹配前次完整数值核验，变化不能沿用旧结果。
产物为`quality/targets/negative_rules/<范围>/chunks/`、observations.parquet及报告
`negative_rule_audit_<范围>.json`。32位置试验为256项；不限位置才是62,000位置
×两年×四任务的496,000项。规则复现不是独立标签精度证据，也不代替OSM几何核验。

实际pilot_32完成256项，无状态或置信度差异，628,574个负例像元与重建一致。
新增5项回归覆盖证据缺失、跨年读取、正例保护、批次边界、置信度、缓存与源变化。
完整工作树615项测试通过（1条既有依赖警告），Black/Ruff、内容门禁及构建通过；
此处仅记录试验完成，496,000项全量审核另行执行。

18:02 UTC核实WorldCover v2完整124,000项通过，5个源接缝冲突均已由原有效掩膜
排除；未修改标签或有效区域。DEM同期85,824/124,000项，8项差异正在排查。
已定位一个位置因旧文件名估计瓦片边界漏读31个边缘高程像元，继而影响32个原有效
坡度像元；继续以真实TIFF范围重建，保留旧结果，不改比较标准来消除失败。


### DEM源边界修正层及共同读取

新增`dem-corrections`：必须先有完整62,000位置DEM/坡度重建报告，拒绝位置限制。
输入绑定原网格、标签manifest、完整数值SHA、几何审核及两段原始DEM源SHA；
重新核实源文件后扫描所有旧高程/坡度值和掩膜。原审核差异位置与“旧坡度的求导邻域
含缺失高程”位置均从真实GeoTIFF范围重建，先复现原差异，再应用完整邻域支持规则。

按新数据目录`targets/corrections/dem/v1/`发布稀疏npz补丁、全位置manifest和
corrections.lock.json。未完成全量像素SHA对账不发布正式锁；重复运行核验来源与
补丁内容，已发布锁保持不变。旧Zarr完全保留，不复制整套标签。共同读取接口
CorrectedDEMReader核对全国网格、原标签及每个补丁SHA，返回高程、坡度和各自
有效掩膜。无修正位置读取原值；缺失邻域对应坡度置无效，而非将填充值梯度用于监督。

当前真实证据：6个异常位置均有真实相交瓦片被旧整数经纬度名称范围排除，缺失
边缘高程引起后续坡度差异；其中最大绝对坡度差78.122度。旧坡度有353个有效像元
依赖缺失高程邻域。原审核unsupported字段基于重建后的有效高程，不能代替这个
旧标签邻域审核。逐位置源范围、目标范围、原比较值和旧实现SHA保存在
`diagnostics/dem_tile_boundaries/findings.json`。

新修正逻辑4项回归覆盖缺失邻域、负高程、全无效、实际补丁/原值读取、原数据保护、
源变化和重复执行；22项相关测试通过。完整工作树619项测试通过（1条既有依赖
警告），格式、内容门禁和构建通过。此处记录代码与测试状态，完整修正实际结果
须另行核验；依赖坡度的可靠负例必须使用修正证据重新处理，不能直接沿用旧负例。

18:10 UTC可靠负例原规则全量496,000项审核完成，无状态或置信度差异；入口
提交bd1ed1c已push并核对远端SHA。该结论只覆盖原规则的计算来源，OSM几何和
修正后坡度的负例仍单独审核。


### DEM全量修正实际完成与依赖负例更新

18:14 UTC，dem-corrections完成全部62,000位置扫描及原始值SHA对账。实际11个
位置生成补丁：6处源瓦片遗漏，加上5处原有缺失高程邻域问题。补回349个高程像元，
补回349个坡度像元，修正353个共同有效坡度值，撤销640个没有完整高程支持的坡度
有效标记；原标签993个坡度像元曾依赖缺失高程。原DEM全量审核最终记录640个
重建后仍缺少邻域支持的坡度像元，不能与原标签的993个混为同一口径。

新增`target-negative-corrections`只处理完整已审核数据，不接受位置上限。先要求
原负例496,000项通过及完整DEM修正锁，再对11个受影响位置×两年重新读取原OSM、
WorldCover、旧坡度与四类负例，每个32位置源块须匹配原审核SHA。使用共同DEM
读取接口取得新坡度后，仅允许water_area规则的状态改变；building、road_all、
waterway必须不变。原正例优先、未知状态和置信度规则保持。

实际完成22个受影响位置年度检查，其中10个需要补丁，共撤销640个水域负例、
新增80个水域负例。产物为`targets/corrections/reliable_negative/v1/`的稀疏npz、
manifest.parquet与corrections.lock.json，报告negative_corrections_full.json。
CorrectedNegativeReader核对原证据块、DEM依赖锁及实际补丁，每次读取明确年份；
原负例Zarr不改写。该修正解决坡度变化的计算依赖，不授予OSM独立几何或精度通过。

真实读取复核包含全部11个修正位置及train/val/test各2个未修正位置，共17位置、
34次年度读取；坡度邻域、负例增删数量和其他三类不变均通过，记录在
`diagnostics/target_correction_readback.json`。全量重新执行的产物稳定性另行对账。
新增3项回归覆盖旧负例撤销、新有效证据、未知/正例保护、真实块SHA、年度读取、
补丁损坏及重复发布；13项相关检查通过，完整工作树622项测试通过，待提交的独立
副本410项测试通过（各1条既有依赖警告），Black/Ruff、内容门禁及构建全部通过。
DEM修正提交23cbb0b已push并核对远端SHA。

吉林一号前6/64包已完成下载SHA校验，解压和增量目录仍进行中；不能将下载数等同
目录数或全量云处理数。旧第3、4包失败已恢复，其余下载继续遵守重试及凭据约束。


### 完整重跑与发布记录

DEM全量62,000位置重跑和依赖负例重跑均退出成功。25个正式产物（2套锁与manifest、
11份DEM补丁、10份年度负例补丁）逐文件SHA完全一致；锁和补丁未重写，2份manifest
重新序列化但内容SHA不变。真实对账保存在diagnostics/target_correction_reproducibility.json。
这验证此次标签修正链的重复执行，不代替B7所需全部影像模态加载与缓存检查。

依赖负例提交0ea9cd0已push并核对远端SHA；独立副本树
e1196a6ce113e1e0ff063149e68b2f73139f3ed8的410项测试及所有格式/门禁/构建通过，
与完整工作树622项测试分开记录，完整日志及SHA已归档至报告目录code_checks/0ea9cd0。
报告和问题清单已更新为已完成的年度标签重建、11位置修正及依赖负例结果；
OSM几何、基础物理合同、完整影像质量与配准、统计索引和最终加载/视觉验收仍未完成。


### OSM几何源重建与边界查询诊断

新增统一入口`osm-geometry`。输入2020-01-01、2021-01-01历史空间索引及当前来源
索引，每份索引SHA须匹配旧标签元数据，同时重新核对其原始PBF/ZIP的SHA、SQLite
完整性、RTree与要素数量、类别顺序和历史日期。当前来源只产生source_bits，不
单独成为历史正例。原标签四类字段及全部30类10m、4类2.5m结构共同核验。

按已有栅格化合同重建：多边形采用all_touched覆盖，线按索引物理宽度缓冲，点按
原记录的高斯范围生成覆盖；10m按4倍采样，2.5m按1倍采样。覆盖量化、历史正例
优先及未知语义保持。原标签值、状态、置信度、来源位分别逐像元比较；16位置一块
累计原值SHA，完整运行必须匹配前次全量数值审核。来源、算法、GDAL/PROJ等运行
版本和实际标签像素均进入缓存指纹；候选文件损坏时拒绝复用。

另比较原足迹查询与按道路半宽、点影响半径外扩后的查询。原中心线/点在足迹外
并不意味着其物理覆盖或热力范围不能影响网格。外扩只作为独立诊断；有变化时保存
`boundary_candidates/*.npz`，不覆盖原标签，也不自动作为正式监督。全量发现的
边界变化须继续核实并连同依赖负例处理。原索引地物分类和独立标签准确性仍属
单独证据，栅格重建相同不能代替它们。

产物：`quality/targets/geometry/osm/v1/full|pilot_32/`的输入锁、分块receipt、
observations.parquet及边界候选；报告osm_geometry_<范围>.json。32位置试验为
128个位置/年份/分辨率视图，完整62,000位置为248,000视图。

实际pilot_32已经完成128视图，原四字段差异0，边界变化0。这不是全国结论。
6项回归覆盖已知多边形面积、道路越界、点热力范围、当前独有地物未知、原始源/
年份校验、全字段对账、缓存损坏和CLI试验范围；14项相关检查通过。
本机直接CLI先加载系统C++库，SQLite依赖要求较新CXXABI，首次入口失败且尚未
处理数据。仅在该审核进程预加载已安装Python运行环境的libstdc++.so.6后恢复，
未改变其他任务环境。实际库路径与SHA留在数据盘；可移植调用示例：
`LD_PRELOAD=/path/to/runtime/lib/libstdc++.so.6 xuannv data prepare-v5 --stage osm-geometry ...`。
完整工作树测试、格式、内容门禁和构建已通过，全量源重建随后运行并单独记录。


### 缺失波段的明确恢复与统一读取

新增`catalog-partial-bands`，输入当前严格目录的波段合同隔离文件、发布分包索引、
分包校验/全量解码记录及全国网格。再次核对涉及原包的完整SHA，对每个文件检查
具名波段、反射率单位、int16、NoData=-28672、逐波段scale=.0001/offset=0、
实际CRS/1280m范围及来源时间。只有已知原生波段集合的严格子集可恢复，未知标识、
重复标识、数值合同不符、空间或观测身份冲突继续隔离；不按文件通道位置补猜波段。

公共读取read_jilin_branch复用已有read_native处理有效值和逐波段缩放，输出仍是
规范的6×256×256、6×128×128或7×64×64原生分支。具名目标波段按标识放入对应
位置，真正缺失的通道为值0、valid=false；标准化仅作用于实际存在通道，不把缺失
通道变成归一化后的有效数值。NoData、有意义零值与负值保持原规则。

补充目录按输入指纹生成`observations/highres/jilin1/partial_bands/<版本>/`，保存
files.parquet、rejected.parquet、files_with_partial_bands.parquet、catalog.lock.json；
current.json指向当前快照。原始影像、严格目录和旧隔离表保留。合并目录为完整文件
显式填写空缺失列表及各布尔状态，避免新增字段为空导致原完整文件被误判。
这项行为先由失败回归复现后修正；原生读取/校准模块未改，现有质量与配准任务的
计算合同未被无关代码更新失效。

实际6包快照核验174文件全部通过“已存在波段”合同：60个目标补充波段齐全，
114个仍缺目标波段；涉及110位置、112场景组，其中2020/2021有71文件、65位置。
明确存在的有效波段观测像元共14,274,560（不是独立地理面积，也不是云后保留量）。
80文件缺少原B5/B4/B6云检测组合，QA和像元融合仍待处理。当前合并目录69,502行，
关键通道状态无空值；不能因元数据恢复就宣布已通过云质量、配准或训练资格。
逐年/传感器/划分统计及缺失波段计数位于diagnostics/partial_bands。

6项回归覆盖缺少非目标重复波段、真实目标通道缺失、归一化后的无效保持、NoData
与零值、未知波段/变动比例、云输入缺失、原包解码前置、空间划分、原始目录保护、
完整/部分记录合并状态和CLI；11项相关检查通过。最终完整工作树634项测试通过，
独立副本422项测试通过（各1条既有依赖警告），Black/Ruff、内容门禁和构建通过。

当前恢复产物仅绑定6包目录快照；新增包进入严格目录后应再次执行此入口，完整
64包的B1产物尚未完成。补充目录与读取接口还需接入最终质量、配准、统计和样本
视图，不能提前把这些后续步骤标记完成。OSM入口8a52575已push且核对远端SHA；
其独立副本416项测试通过，真实全国扫描继续运行。

### 补充目录的稳定版本锁

已先复现相同输入重跑导致catalog.lock.json仅因完成时间变化而改变SHA的问题。
正式锁移除运行时间，运行报告仍保留每次完成时间；相同输入下锁和current指针均
不重写，锁内容冲突必须使用新版本。输入指纹增加NumPy、Pandas、Rasterio、GDAL
和PyArrow版本，避免不同运行库产物混用。

实际6包补充目录再次生成并重跑：版本c92f204ae5840fee421b，5个正式文件SHA全部
一致，锁与current指针修改时间也保持不变。数据盘diagnostics/partial_bands/
reproducibility.json保存逐文件前后SHA与时间；执行日志单独留存。修正后10项相关
测试、全树Black/Ruff、仓库门禁及发行构建通过。此前完整634项和干净副本422项
覆盖38b1822，其GitHub远端SHA已核实；不把此前全套结果冒充本修正后的测试。

### 后续分包自动更新补充目录

续处理顺序增加为：新包严格目录→缺失波段补充目录→当前完整5m波段内部审核→
64包齐全后的正式云流程。每轮核对补充目录的源锁、全国网格、隔离清单、严格
目录、实现/公共读取/网格代码、运行库、正式锁及两份输出表SHA；旧目录、损坏
输出或算法变化不能被当作当前完成。补充步骤失败仍遵守同输入仅尝试一次，
记录失败并保留其他独立数据任务；不循环重试或跳过失败宣布完成。

先加入失败回归，覆盖新批次、缺失/变动/损坏产物、正确执行顺序与失败退出；
15项相关检查通过，完整工作树637项通过（1条既有警告），独立副本425项通过，
Black/Ruff、仓库门禁及构建通过。现有6包正式快照的完成检查实际返回true。
本次衔接解决后续包补充目录遗漏；部分波段尚未纳入云处理、绝对配准或训练统计，
完整5m内部审核的完成不代表部分波段也已通过。

OSM独立诊断另冻结47,488视图的中途快照，其中140视图有边界候选变化。按历史
新增候选像元选6个不同位置并加1个仅来源位变化视图，重新读取原Zarr逐字段核对
差异与候选SHA，形成7张前后对照图。当前独有地物仍为未知；候选未经正式标签
批准。材料位于diagnostics/osm_boundary_snapshot_20260913_1928，不能替代全国
审核或C1要求的影像/云质量96组样例，也不是独立标注准确率验证。

### 19:32 UTC执行核实与交付记录

本轮4项代码改进均已单独push并核对远端：8a52575（OSM源几何审核）、38b1822
（保留缺失产品的已核实通道）、9d889f6（重复运行稳定锁）、22d3b34（新增分包
自动更新补充目录）。详细检查日志、隔离副本代码树和SHA归档到数据盘code_checks；
运行记录goal_iteration_20260913_osm_partial_bands.json关联实际数据锁与诊断。

仅在确认旧续处理器没有子任务时替换该处理器，已核实新进程身份和实际状态：
当前6包partial_catalog_finished=true，无失败记录；下载、基础完整性、高分波段
审核、OSM源几何进程均维持原身份并存活。第7/8包分块分别约2.557/2.503 GB，
相对先前2.380/2.325 GB继续增长，不能把这些分块字节计为已完成包。

当前吉林一号6/64包、严格目录69,328文件，另174文件补充合同已核实；当前完整
5m分支10,466观测内部审核完成，不包含部分波段，不代表绝对配准合格。高分内部
审核已处理24,832/177,505观测；全国OSM完成12,848/62,000位置、51,392视图，
原标签重建差异0，148视图存在边界候选变化；基础完整性20/72包完成。上述全国
任务尚未全部完成。年度静态标签和DEM/依赖负例已生成修正的结论继续保持。

现有基础影像导出合同缺证、云视觉审核、绝对配准、完整统计/索引/加载及C1材料
仍未齐，open_issues.json与current_execution_status.json已同步实际情况。验收
保持incomplete，不以代码检查、部分产品合同恢复或作业存活代替完整数据验收。

### 吉林一号逐波段QA：缺失通道不再清零其他通道

新增统一入口`jilin-quality`。输入当前已校验的完整/部分波段合并目录，固定其
不可变目录版本和2020/2021全部场景。5m云输入仍为B5/B4/B6；仅这3个通道共同
有效的位置送入冻结模型。非云输入通道的NoData不再误作整景云输入缺失。每个
目标原生分支分别保存C×H×W的data_valid和valid，最终valid是逐波段原始有效性
与地理投影后QA的交集，缺失波段始终false。细到粗使用保守重投影，保持30m云/
云影缓冲及低于60%仍保留清晰像元规则。分支晴空面积按至少一个可用目标通道
统计，同时保存每通道有效数和比例、完整通道标志，禁止把该比例冒充全部通道齐全。

无同景5mMS、缺B5/B4/B6或这3通道无共同有效像元时，显式记录QA缺失原因；
不使用B0或低分辨率补充光谱冒充5m云输入。原始逐波段数据有效性仍保存，训练
质量掩膜保持false。未知QA类别单独用255标示，已推理类别仍为0/1/2/3。

原QA保留，新配置在quality/cloud/jilin1/v2/<配置指纹>/生成classes.zarr、
valid_masks.zarr和逐场景receipt，目录快照在catalogs/<目录指纹>/生成输入锁、
observation_quality.parquet及正式质量锁。源文件SHA逐个复核；缓存复用重新检查
所有分类/置信度/掩膜数组SHA。模型、读入/掩膜/推理代码、运行库和目录均绑定
版本，输入变动或数组损坏拒绝复用。模型配置相同的后续目录可复用已核验场景。

32场景真实试验优先选择部分波段场景：128分支中34个缺目标通道，其中24个仍
有合格QA下的可用通道，共109,469有效波段观测像元；这不是独立地理面积，也
不代表云检测准确率提高。30场景完成冻结模型推理，2场景缺云输入，8个对应分支
全部保持训练质量无效。128分支逐项验证valid没有超出data_valid，逐波段计数一致。

实际重跑复用32/32场景，全部缓存数组与receipt检查通过，质量锁SHA保持
759b3b711bfee62eaa70ffbd6e7cd4524685fee2b00ce1b6238833e9ce7f84a1。数据盘
diagnostics/jilin_per_band_quality_pilot32保存验证及4组掩膜对照。亮地物疑似云误判
仍需核查；对照仅证明正确保留通道，不作为独立云真值或C1完整96组材料。

7项新增回归（含参数化）覆盖部分通道、非输入通道NoData、低晴空率、缺MS/
缺NIR、非法预测、源变动、损坏缓存、稳定重跑、试验优先覆盖缺失波段及CLI。
18项相关检查、644项完整工作树测试及432项独立副本测试通过（完整套件各1条
既有警告），Black/Ruff、仓库门禁和构建通过。CLI回归初次接入后发现测试缺少
源锁隔离而访问网络，已停止该测试进程并补齐隔离；未改变实际数据作业。
本阶段只更新数据掩膜，不更新嵌入模型。当前32场景检查不是全量完成。

### 每批目录自动衔接新版QA

续处理器采用：严格目录→部分波段目录→当前完整5m内部波段审核→jilin-quality。
每批已下载目录都执行新版QA，后续批次复用已验证场景，不再只等64包后执行旧的
全通道交集流程。完成判断绑定当前补充目录版本、质量锁、整批场景数量、无试验
限制的输入锁、配置位置、模型/代码/运行库SHA及质量表SHA；32场景试验不能满足
整批完成条件。QA流程完成与分支质量合格分开，缺失QA结果不获得训练资格。

续处理器登记已有jilin_quality_v2进程身份，在其存活时避免重复启动云推理；当前
全量工作进程仍处理固定6包目录的11,192场景，新目录形成后再续处理增量。其他
下载及CPU审核任务继续运行。失败仍按原同输入一次尝试规则报告，不无限重启。

18项相关检查通过；完整工作树646项、独立副本434项测试通过，Black/Ruff、
仓库门禁及构建通过。实际128分支从公共read_jilin_branch带QA读回，valid与
保存掩膜完全一致，无效值保持零且全部数值有限；该检查未使用尚未生成的训练
统计量，也没有调用嵌入模型。证据reader_integration.json留在数据盘诊断目录。

### 19:53 UTC实际进度与提交核验

本轮b95814c（逐波段QA）和046f6cb（每批自动QA）已分别commit、push到wwj并
核对远端SHA。检查归档位于code_checks/b95814c和code_checks/046f6cb；本轮执行
记录为goal_iteration_20260913_per_band_quality.json。新的续处理进程已经识别
已有云任务并保持jilin_quality_finished=false，未重复启动设备推理。

吉林一号下载校验达到8/64包；第7包已解压、解码未完成，第8包待后续解压解码，
正式目录仍是6包。第3/4包实物字节数符合发布值，解码记录分别11,679/11,547个
TIFF且发布SHA一致，旧失败已恢复，不再次启动这两个包。新版逐波段QA已处理
2,784/11,192场景，其中2,604场景推理、180场景显式QA缺失；全量尚未完成。
高分内部波段审核28,160/177,505；OSM源几何19,632/62,000位置，原标签对账
尚无失败，249视图有边界候选变化；基础完整性22/72包完成。所有相关运行进程
身份均重新核验，无模型训练授权，最终数据验收仍为incomplete。

### 原生波段审核的执行性能优化

固定5个高分传感器变体各8个位置，共40张真实影像，使用已有通过的v5校准、
同一原生读取和inspect_intraband算法比较执行方式。2线程14.795秒、4进程9.203秒、
8进程6.278秒（均含本次执行器启动/退出），8进程约为原方式2.36倍吞吐；所有
逐窗口/逐波段配准结果与状态完全一致。该小样本吞吐不是全国完成时长保证。
另将40份已有receipt复制到诊断目录，用8进程核对实际源字节后全部复用，与直接
算法结果一致，副本SHA未变化，原正式receipt只读。诊断绑定源清单、校准和实现SHA。

新增CLI入口band-alignment-parallel，支持1—16进程，部署先采用已测试的8进程。
复用原calibrate_family验证算法、参数、运行库、固定样本及真实源文件；原
v5_intraband/v5_alignment/v5_rasters未修改，原校准和已完成receipt继续有效。
每进程限制原生计算库为1线程，使用spawn并分128观测批提交；保持输出顺序和
原始判定规则。另记录执行器SHA、进程数及线程限制，不能将执行优化冒充精度提升。

并行入口与原band-alignment共享同一个按传感器互斥锁，拒绝同时写同一产物。
复用时仍重算实际源文件SHA；文件变化明确拒绝。正式完成前再核对校准、算法和
执行器未变。进程中断后保留原子发布的receipt，重跑重新核对后复用，不取消云任务
或更改原始影像。无效纹理、不确定和超限结果仍分别保留，像元融合不自动授权。

3项新增回归覆盖原receipt兼容/源变动拒绝、真实TIFF多进程与直接算法一致、
稳定重跑/校准不变、CLI及旧写锁冲突；8项相关测试通过。真实性能及复用证据
位于diagnostics/native_band_throughput。后续实际全量吞吐另记，不能用40样例
验证代替177,505观测的全量审核。

本次执行器完整工作树649项、独立副本437项测试通过（各1条既有依赖警告），
Black/Ruff、仓库内容门禁及发行构建通过。

### 并行执行已部署及后续批次衔接

执行器e6b986f已单独push并核对远端SHA。高分全量审核确认旧进程退出后换为
8进程；原校准SHA保持不变，30,203份已完成receipt全部重新核对后复用，继续
审核177,505观测的剩余部分。旧进程身份、切换前数量和校准SHA保存在诊断目录。
同一正式产物没有同时存在两个写进程。

续处理器的后续吉林一号内部审核改用band-alignment-parallel，继续遵守目录、
补充波段、内部审核、逐波段QA的前后顺序；已运行子任务的结果依然有效。此路由
改动先由失败回归复现，14项相关检查、全树Black/Ruff、仓库门禁及构建通过。
649项工作树和437项干净副本结果对应先前e6b986f执行器，不冒充本次路由修改后
重新运行的全套测试。运行时采用8进程，并分别记录设备云任务和CPU审核进程。

### 2026-09-13 20:19 UTC新产物与执行证据

新版续处理器保留并登记了已运行的吉林一号第7包审核子进程，身份保持不变；
该任务完成后，实际启动第8包目录的band-alignment-parallel，8进程参数与独占
写锁均已核实。原高分、吉林一号校准未变，云模型与数据影像未改写。

固定6包目录的逐波段QA现已完整处理11,192场景、42,244唯一分支，其中10,466
场景已推理、726场景明确QA缺失。质量锁SHA为
249df4561442f8ebd09a14b491d65f480099a579553a98c02fea3261c96facff，质量表
SHA为89b5653bd6eceed74b11220f4ff39c856ce42ea020440f755bec51f898cd9538。
缺失QA分支无available=true记录；29,995分支至少有一个质量掩膜下的可用通道，
这不表示通过绝对配准或云准确率评测。6包输出快照与当前8包目录严格区分。

8包严格目录92,563文件，另233个不完整文件的已有通道已核实：80个目标分支
齐全、153个仍缺目标通道，100文件属于2020/2021训练年份。当前高分内部审核
38,912/177,505，OSM几何26,640/62,000位置，
基础完整性24/72包。高分在约339.6秒内新增审核3,840观测，
实际吞吐11.3观测/秒；这是所测时间窗口，不外推全国耗时保证。

完整检查、40样例性能、40旧receipt复用、切换及子进程保留证据归档到
code_checks/e6b986f、code_checks/c045051和diagnostics/native_band_throughput。
独立副本的公开xuannv data prepare-v5入口已确认包含并行审核及新版QA命令。
当前内部审核仍需结合逐波段QA约束清晰地物；未核实基础辐射合同、绝对配准、
统计/索引/加载和完整视觉材料继续保持未完成。验收仍incomplete，无训练授权。

### 2026-09-13 吉林一号QA运行异常及恢复

8包目录QA在20:16启动后未产生新进度，进程持续消耗内核时间，其他线程阻塞于
内存映射。准确根因尚未确认。保存线程诊断及失败记录，正常终止无效后，仅结束
该已确认身份的异常子进程；20:29确认退出。6包正式质量锁SHA仍为
249df4561442f8ebd09a14b491d65f480099a579553a98c02fea3261c96facff。
没有重置整机设备或改写原始影像，下载、CPU审核继续运行。

确认写锁释放及设备6空闲后，以相同模型、配置b17a357ecf8aaa89e8db、5米B5/B4/B6
输入和30米缓冲运行32场景恢复验证。20:37完成，25场景复用缓存，7场景实际新推理；
合计31场景模型推理、1场景QA缺失，128分支。逐一核验576个缓存数组摘要、32份
receipt及源文件SHA，公共读取器的128分支有效掩膜与落盘结果一致，无效值为零、
数值有限，QA缺失分支保持不可用。此检查没有应用尚未生成的训练统计量。

恢复试验使用8包补充目录61a544d7f9d43d2e1403，QA快照48917e406dafba98ef5f；
质量锁SHA为ca467da149914c9e669edaafaa7a37b9bea634625cc8f87ada09e2eb01f8c1b1，
质量表SHA为65d4d18a4a7a6864ca19c543591e5f29b0be59966e568318aa350a1e482a7ae5。
原始失败、退出证据、试验及读回保存在报告目录diagnostics/jilin_qa_runtime_stall。

20:38启动设备6完整QA，并在确认原协调进程无子任务且已退出后登记新QA身份，
重新启动续处理器，后续批次也使用设备6。此前exit_code=-9的记录单独归档，
不把重启后的空失败列表当作历史无失败。20:41完整QA实际推进到3,712/14,902场景；
这仍是当前8包范围，既不代表64包完成，也不表示云识别准确率或绝对配准已验收。

同期高分内部审核54,400/177,505，OSM几何32,288/62,000位置，原始重建0差异、
535个边界候选视图，基础完整性28/72包、0失败。所有数量都是当时检查的进度，
后续继续以实际产物与校验值核对，不沿用旧进程存活结论。

### 清晰地物上的波段内部配准校准

新增xuannv data prepare-v5 --stage clear-alignment-calibration；必须提供sensor-family、
alignment-version和quality-root。高分quality-root指向完整cloud/gaofen，吉林一号
指向已发布完整批次的catalogs/<snapshot>，32场景试验不能充当完整QA输入。
入口使用独立的按产品写锁，不覆盖运行中的原生v5审核。仅CPU校准，不调用模型。

输入固定为原v5校准的40张高分、16张吉林一号训练样例；重新核对原校准、原始
源文件及全国划分。高分按已排序观测ID定位压缩掩膜，核对来源、完整标记、存储
参数，并由云分类和原生有效掩膜重算30米缓冲，与落盘掩膜逐像元比较。吉林一号
核对完整QA表、源目录及其锁、scene receipt和逐波段掩膜SHA。通道顺序、形状、
源SHA及空间划分不一致时拒绝读取，保留NoData和缺失通道的无效状态。

处理为原生逐波段有效掩膜与QA掩膜相交，调用未修改的v5边缘匹配和已知平移
校准算法。整景不足60%晴空不清零；仍需每个匹配窗口达到原有有效面积和纹理
要求。全部有云、参考通道无效或清晰纹理不足只能报告uncertain，像元融合授权
始终为false。结果同时保留原始和QA约束后的逐窗口判定。

输出在新数据目录quality/alignment/clear_intraband/<family>/<输入与掩膜版本>/
calibration.json。绑定实现、运行库、旧校准、样例表、QA目录与逐样例掩膜摘要；
完全相同输入重复运行不得重写正式结果。报告保存在clear_alignment_calibration_*
以及diagnostics/clear_intraband_calibration。

本次真实56样例、224个人工平移测试：高分72个、吉林一号7个获得可靠位移估计，
这些估计无超出既定误差或错误阈值判定，但其余案例清晰纹理不足。高分各变体完整
通过的样例数为GF1=4、GF1B=2、GF1C=2、GF1D=6、GF6=4；吉林一号GP01=0、
GP02=1，未满足每变体至少4位置的门禁，两族均为insufficient_clear_calibration。
不能据此发布“已完成清晰地物配准校准”或把不确定结果算作合格。

同一56样例的波段间测量：高分17个原始passed变为uncertain，13个仍passed，
9个仍uncertain，1个uncertain变为passed；吉林一号2个over_limit变为uncertain，
1个仍over_limit、13个仍uncertain。此变化说明清晰地物证据不足，不证明云检测
准确或绝对配准改善。下一步需单独冻结按QA覆盖筛选的训练校准样例，保留本次
原始固定样例结果，不按已测配准结果挑选通过者，也不降低误差与置信度门槛。

8项新增回归覆盖低于60%整景仍可使用清晰窗口、云内任意值不改变配准、缺失通道、
无QA、通道/类型/形状错误、观测顺序置换、源目录与掩膜破坏、来源校准不足、
固定结果重复执行及CLI写锁。23项相关测试通过，完整仓库检查另外记录。

完整工作树657项、独立干净副本445项测试通过（各1条既有依赖警告），Black、
Ruff、仓库门禁与发行构建通过。另生成8组原图、QA有效区域和配准窗口对照，
位于diagnostics/clear_intraband_calibration/index.html；这8组不是C1规定的96组。
查看山区样例时，部分带明显地形纹理的明暗区域被QA排除，需继续审核积雪、
地形阴影与云影混淆；单波段图不足以建立云准确率真值，不自动翻转掩膜。

后续校准候选已在测量前冻结：只从train选参考波段QA有效面积至少95%的观测，
每变体8个不同位置，2020/2021各4个，用固定种子与观测ID哈希排序，不读取任何
已测位移或配准状态。得到40个高分、16个吉林一号候选；目前仅完成候选清单，
尚未在这组候选上运行校准，不以选到晴空样例宣称校准通过。

实现898cfd5已push并核对远端SHA。提交后通过统一CLI重跑全部56个原校准样例，
两个正式结果的SHA和修改时间均保持不变，证明没有重写锁定产物；两族仍为清晰
校准不足，没有因重跑改为通过。执行记录绑定该提交，证据在
diagnostics/clear_intraband_calibration/real_replay_verification.json。21:08核实时，
高分原生审核74,496/177,505，OSM几何34,864/62,000、614边界候选视图，
基础完整性32/72包、0失败；第9、10包下载均有近期分块写入。数据总验收仍未完成。

### 按年度固定晴空训练样例的校准

新增xuannv data prepare-v5 --stage clear-training-calibration，参数为sensor-family、
quality-root及原alignment-version。原始56样例的不足结论和产物保留；新入口单独
使用清晰训练集合，仍采用原v5匹配算法、误差界限及每变体至少4位置的校准门禁。
质量输入为完整高分QA和已冻结的吉林一号8包QA快照ed5e008401e2c8a89f9e。

候选只使用完整原生多光谱分支，参考通道高分green、吉林一号B4的QA有效面积
至少95%；每变体8个不同位置，2020/2021各4个。按固定种子
clear-native-calibration-v1与观测ID的SHA256排序，缺少合格训练位置时失败，
不以验证/测试位置补齐，不读取或使用已测配准状态。代码选出的40高分、16吉林
一号ID与此前在测量前冻结的候选清单逐项一致。

匹配之前，逐文件核对原生影像与QA，确认实际参考有效面积满足条件；随后发布
inputs.parquet和inputs.lock.json，锁定清单、源/QA版本、每个掩膜摘要、原校准、
实现、运行库及选择规则。输出位于新数据目录quality/alignment/clear_training/
<family>/<输入版本>/。完成所有已知平移和实际波段间测量后，再读回所选源文件与
掩膜，确认未变才发布calibration.json及output.lock.json。缓存复用也重新核验
源文件、掩膜及结果SHA；中途输入变化或校准结果被改写必须拒绝。进度明确记录
execution_status=finished，方法校准状态与执行是否结束分别表示。

真实40张高分的160个已知平移案例全部通过，各变体8个位置全部通过；最大误差
0.025像素。真实16张吉林一号的64个案例全部通过，两变体各8位置通过，最大误差
0.075像素。两族方法校准状态均为passed。原始无QA校准、原固定56样例加入QA
后的不足结论、此次晴空训练校准分别保留，不混作同一个样本集的精度提升。

实际跨波段结果仍须单独看待：40张高分为15通过、24无法可靠判断、1超限；
16张吉林一号为15无法可靠判断、1超限，没有整组通过者。超限吉林一号的B6相对
B4估计残差约6.803米，B1则存在窗口间不一致，不能将其不可靠估计当作已证实偏移。
方法校准通过不批准这些影像参与像元融合，也不证明绝对地理配准或云检测准确。

5项新增回归覆盖年度均衡/空间划分、输入顺序和已测状态不影响选择、训练位置
不足拒绝、匹配前冻结、缓存结果破坏拒绝、处理中源变化拒绝、重复执行与CLI写锁。
13项相关测试通过。真实输入/输出核验位于diagnostics/clear_training_calibration，
全量内部配准、基础参考及最终数据验收仍需继续完成。

完整工作树662项、独立干净副本450项测试通过（各1条既有依赖警告），Black、
Ruff、仓库门禁及发行构建通过。通过统一CLI实际重跑两族校准，均重新核验源与
QA后复用结果；每族4个正式文件共8个产物的SHA与修改时间全部不变。结果及锁
位于上述clear_training目录，校验和缓存证据位于
diagnostics/clear_training_calibration/real_replay_verification.json。

新增4组晴空样例原生波段、边缘及有效掩膜对照，位于
diagnostics/clear_training_calibration/index.html，包含两族超限和其他状态样例。
只显示原生数据，不自动平移影像；这些诊断不计作C1的完整96组验收材料。

### B3：把晴空方法校准接入真实观测审核

新增统一入口`xuannv data prepare-v5 --stage clear-band-audit`，明确传入
`--sensor-family`、`--quality-root`和`--clear-calibration-root`；`--workers`限定1—16，
部署先以4进程核验，正式采用8进程。`--max-scenes`仅用于独立标记的试运行，不传
该参数才审核冻结质量目录中的全部原生多光谱观测。每个传感器族独占写锁。

输入为已发布的晴空训练方法校准、该校准原质量目录、当前完整QA快照及其来源
目录。先核验校准输出封印，并重读校准样例源文件和实际掩膜；当前QA参数必须
与校准时一致，新增传感器变体必须先校准。吉林一号后续包可以使用同规则、已完成
的新QA快照，但各快照输入单独冻结，不能将8包目录声称为10包处理结果。

审核不按95%校准样例阈值、60%整景阈值、空间划分或波段完整性筛选观测。高分
审核原生4波段8米MS；吉林一号审核原生6通道5米MS，缺失通道仍保持无效，
已存在的通道继续测量。参考波段缺失、移动波段缺失、无共同晴空像元分别记录原因。
保留每对波段的偏移、可靠性和窗口证据；整组仍按可靠超限优先、否则有不确定则
不确定、全部通过才通过的原规则。B0、PAN及10/20米补充分支不冒充已校准的5米
MS，跨分辨率与绝对参考审核仍待完成。

输出为新数据目录`quality/alignment/clear_audit/<族>/<算法版本>/`下的逐观测
receipts和catalogs/<输入快照>，后者保存inputs.parquet、inputs.lock.json、
observations.parquet及output.lock.json。计算前冻结清单；每次复用都实际校验
源像元与质量数组，结果摘要改变时拒绝复用。处理后再次核验实现、输入和方法校准，
只有核验通过才发布完整快照。报告将执行结束与影像是否合格分开；
`pixel_fusion_authorized`和`training_authorized`始终为false。

真实试运行各32张：高分12通过、20无法可靠判断；吉林一号1通过、31无法可靠判断，
两组无拒绝读取。吉林一号此次明确使用已完成的8包质量快照（13,926张5米MS候选），
高分输入候选177,505张。这64张是流水线验证，不是全量配准结论。各文件校验、重跑
证据和部分波段诊断留在报告目录diagnostics/clear_band_audit。

新增回归先以缺少实现复现失败，随后6项通过：全划分/多云保留、缺失波段和参考、
实际QA数组破坏拒绝、缓存结果篡改拒绝、校准封印必需、独立CLI锁及多进程重跑
正式产物不变。测试中的人工平移约17.9米样例确认为超限，未为满足断言改变算法。

64张真实影像改用2进程重跑，全部复用，8个正式文件SHA与修改时间均不变。
另外实际读取当前8包QA内全部4张缺波段5米MS，4张均为无法可靠判断，缺失通道
及其余波段结果均保留。证据分别为real_replay_verification.json及
actual_partial_native_audit.json，不能据此宣称缺波段问题已全部修复。

吉林一号10包云处理再次出现运行库初始化的内存映射阻塞，尚无新增进度时保存
线程栈和进程身份，终止无响应实例后从原缓存恢复。替代进程已产生新推理结果；
此前8包正式质量产物保留。故障证据及失败退出记录位于
诊断目录jilin_qa_runtime_stall_10，准确运行库根因仍待确认。

本次完整工作树668项测试通过；从暂存树构建的独立干净副本456项通过，各有1条
既有依赖警告。两处Black、Ruff、仓库门禁和发行包构建通过；只提交此次审核入口、
其测试及实施记录，其他既有未提交修改保留。

### 本轮实际执行结果与GitHub记录

审核实现提交`210149d95ad695d357c4919b6f4812bb874dc3f9`已push到wwj并核对远端SHA。
高分完整177,505张QA约束组内审核已经产生新结果，沿用试运行校验过的实现和
已通过的晴空方法校准；正式完整结果尚待运行结束发布。

吉林一号10包QA于2026-09-13 21:58 UTC完整结束：18,647场景、70,329分支；
17,410场景完成模型推理、1,237场景显式QA缺失，复用14,902场景。
冻结质量快照为6d3b2a6d3b11b521baac，补充目录版本a01bd29b8faedb8e5425，
质量锁SHA为45213037baca08c664e19fd6aef5ea181e2173c8d1c4d2c6bc7e92a17d7042a1，
质量表SHA为351bbf75003b69d1567e99bcd14e0071bfa7939b71e86ab692801d18922173de。
校验后已启动该冻结快照17,414张5米MS的完整组内审核，包含4张缺波段影像。
这仍是10/64包的数据范围，云分类需要可视化复核，绝对配准及最终统计、索引、
加载验收尚未完成，不能记录为数据已通过验收。

64张试运行与相同源文件的旧原始掩膜结果对照：高分17张由通过变为不确定、
12张仍通过、3张仍不确定；吉林一号6张由超限变为不确定、1张由通过变为
不确定、1张仍通过、24张仍不确定。这里只报告QA改变可测范围后的实际状态，
不将其解释为配准真值或准确率提升；原记录及对应SHA保留在诊断目录。

### B5：高分辨率训练统计与六类原生分支共用读取

新增`highres-statistics`统一入口，要求传入传感器族和已完成、冻结的质量目录；
`--max-scenes`只生成标记pilot的前缀试运行，不传则处理该目录全部训练观测。
高分MS/PAN及吉林一号5米MS、B0、10米和20米补充光谱共享原生QA读取接口。
逐文件源SHA、逐波段掩膜及QA收据均核验；高分PAN另外按实际地理变换重建同景
MS掩膜传递，与存储结果比较。缺失和无效通道输出零值及false掩膜，不能参与统计。

高分采用已核实的native_stored_dn合同，MS/PAN按产品与卫星变体分开；吉林一号
反射率按元数据只缩放一次，各分辨率仅选择约定光谱通道，不重复计算同名波段。
不把有效反射率统一裁剪到0—1。统计使用公共读取所得掩膜；这批产物是QA约束下
逐波段数值统计候选，绝对配准与视觉验收仍是独立前置条件，不自动授权归一化训练。
最终训练视图如增加筛选或改变掩膜，必须生成与新视图匹配的新统计版本。

仅train进入清单；不同划分出现完全相同源内容时停止并报告，同划分同产品/变体
的重复内容登记为别名排除。输入清单按观测标识固定排序，年度先验没有季度复制项。
按产品、传感器、波段流式累计count/mean/M2/min/max，方差ddof=0；分位数使用
固定种子42、每波段最多4096个有效像元的随机优先级样本，明确为近似分位数。
有效数不足2、标准差非有限或不超过1e-12时，产品失败并列出失败通道，禁止生成
虚假标准差。有效像元的非有限数在统计入口拒绝，不能被无声过滤。

输出位于新数据目录statistics/train/highres/<族>/<输入版本>/：
inputs.parquet、inputs.lock.json、excluded.parquet、source_and_masks.parquet、
contributions.parquet、statistics.json、products/<产品>__<变体>.json及output.lock.json。
地区定义为经纬各5度固定格子，报告每地区/年份/波段的观测数、非空观测数、有效
像元数及该产品变体通道的像元贡献比例，避免只看全国均值掩盖密集覆盖地区的影响。
发布前检查代码、源质量目录及输入锁；所有输出校验值由最后写入的共同锁绑定。
重跑已发布结果时重读实际源与掩膜并核验，但跳过统计运算，不改写有效正式文件。

7项新增回归加2项原统计回归通过，覆盖跨划分/重复内容、四种吉林一号原生形状、
缺失QA、PAN地理掩膜传递与破坏拒绝、小数组直接计算、空通道失败、重跑与CLI锁。
实际各128观测试运行完成：高分覆盖4个产品/变体组合，吉林一号覆盖8个组合，
无失败产品。冻结目录完整训练候选分别为284,230和55,977个原生分支；其余70,780
和14,352个非训练观测不进入统计。这些128条试运行不代表全国统计已完成。

实际256条分支按产品分别拼接有效像元做独立直接计算，流式与直接计算的有效数
完全一致，均值/标准差最大绝对差分别约3.41e-13、2.84e-13；地区贡献像元数与各
通道总数对账一致。格式修正后重新生成版本，统计数值文件与该直接核验版本逐字节
一致，再重跑公共读取，26份正式清单、产品统计及结果锁的SHA/修改时间均未改变。
证据位于报告目录diagnostics/highres_statistics/real_direct_verification.json及
real_replay_verification.json。这是统计流水线与缓存核验，不是云分类真值验收。

完整工作树675项、最终独立干净副本463项测试通过（各1条既有依赖警告），
Black、Ruff、仓库门禁和发行构建通过。仅提交此次公共高分辨率读取、统计入口、
对应回归及实施记录；现有未提交的模型、训练和旧流水线修改保持原样。

### B3：逐窗口失败诊断，保持原审核判据

新增统一入口alignment-diagnostics，显式传入--sensor-family、--quality-root和
--alignment-audit-root；输入必须是完整发布、带封印的原生组内审核及其同版本QA。
先核验观测表、原输入和全部实现校验值，重放所选观测时要求逐对结果与已发布审核
完全一致。诊断不改写源像元、掩膜、窗口位置或配准判据。

选择仅限train且原审核不确定者，按卫星变体、2020/2021、B4参考覆盖率五档
（零、0—25%、25—60%、60—95%、95%以上）分层，每档固定哈希顺序最多4个
不同位置。实际吉林一号选80张；选择清单在逐窗口解释前写入inputs.parquet和
inputs.lock.json，含运行库、代码、源审核与QA版本。该集合用途是诊断，不是
校准或评测集；分层计数不能直接推成全国发生率。

对原4个固定窗口分别记录有效比例、边缘标准差、整数峰相关性、峰值竞争间隔及
细化匹配结果。原因区分有效面积不足、纹理不足、无可用相关候选、相关性不足、
峰值歧义、细化拒绝和接受；接受数量必须与原审核一致。另用积分图仅统计所有
允许原点的64像素窗口覆盖，保留原16像素搜索边距。发现内部有效窗口不等于有
3个独立、可靠匹配窗口，不能据此将观测改为通过。

真实80张的400对波段共1,600个角落窗口：925个有效面积不足、275个整数峰相关性
不足、7个峰值歧义、393个被原算法接受。有5张影像的25对波段在角落都不可用时，
内部仍有达到70%覆盖的窗口。这支持后续独立试验根据有效区域选择窗口；相关性
不足需要另一项假设，不能只移动窗口或降低阈值解决。

这80张原始共同有效覆盖几乎100%，各覆盖档的低比例主要来自QA之后，而非源文件
大片NoData。7组原图/原始有效性/QA有效性/角落与内部窗口对照留在
诊断目录native_alignment_causes/index.html；图上黄色框只是最大有效面积位置，
没有新增配准合格结论。雪、亮地物或阴影是否误判仍需独立复核，不自动翻转QA。

输出位于报告目录alignment_diagnostics/<族>/<输入版本>/，包含输入清单/锁、
diagnostics.json和output.lock.json；处理中来源、输入锁或实现变化时拒绝发布，
重跑要求正式结果相同。5项回归覆盖原决策一致性、周期纹理歧义、中央有效/角落
无效、训练分层选择、CLI独立锁、先冻结再测量、结果篡改及运行中输入锁变化。

吉林一号当前10包的55,977个训练分支统计于2026-09-13 22:30 UTC完整发布，
8个产品/变体组合均无失败；14,352个非训练观测排除，未发现被重复计数的源内容。
完整来源锁、12个被封印的输出文件和各地区贡献合计已核验，完整只读缓存复核
另行运行中。统计版本efd8b74c9d4b7738f607的输出锁SHA为
8e3673a326587fed57d364adcb0de988b43535ca31fd405ccad34a983b672a63；
证据位于diagnostics/highres_statistics/jilin10_full_verification.json。
这仍是当前10包QA下的数值统计候选，非64包或最终训练视图统计验收。

最终版本重复读取80张源文件和实际QA，逐对审核与诊断均一致，4份正式输入/输出
文件SHA及修改时间不变，证据为native_alignment_causes/real_replay_verification.json。
完整工作树680项通过；最终输入锁变更回归5项及最终独立干净副本468项通过，
两套完整测试各1条既有依赖警告。Black、Ruff、仓库门禁、发行构建通过。
诊断样例集只支持定位问题，尚未更改配准算法或新增任何合格影像。

2026-09-13 22:49 UTC，吉林一号10包完整统计缓存复核结束：55,977个训练分支的
源文件与实际QA再次核验，跳过统计运算；15份正式输入、输出和产品文件的SHA
及修改时间全部不变。证据为diagnostics/highres_statistics/
jilin10_full_replay_verification.json。此项复核已完成，最终训练视图与全64包
统计仍需后续版本，不能据此宣布整套数据已可验收。


## B3 候选窗口布局标定（2026-09-13；实际结果另列）

输入是已发布clear_training的原始56张训练样例、原QA目录与来源校验值。
不从此前80张失败诊断样例重新选择标定输入，不依据候选偏移挑选样例。
新增入口 `xuannv data prepare-v5 --stage adaptive-alignment-calibration`，
必须显式提供通用四个根目录、sensor-family、quality-root和clear-calibration-root。
各传感器族使用独立运行锁；输出到quality/alignment/adaptive_calibration/族/指纹。

1. 验证原标定已通过、四文件封存、原代码、QA文件、训练划分及逐观测来源证明。
2. 在首次测量前发布候选inputs.parquet及inputs.lock.json，固定原56个观测ID。
3. 仅根据参考波段自身有效掩膜与边缘纹理选窗口，不读取移动波段相关性或偏移。
   原四角至少3个窗口达到70%有效比例且边缘标准差≥0.0001时保留原布局。
   否则枚举四块互不重叠64×64模板组成的128×128布局，保留16像素搜索边距；
   按达标窗口数、达标窗口最小覆盖、达标覆盖总和、纵横坐标依次确定唯一布局。
   每对波段最多尝试该布局的4个窗口，匹配失败不改选其他布局。
4. 匹配仍使用原边缘NCC、相关性≥0.65、至少3个可靠窗口、窗口偏移偏差≤0.5像素、
   残差≤5米与原峰值歧义阈值。模板不重叠，不用重复小区域凑齐独立证据。
   网格至少160像素，以便容纳独立模板及搜索边距；不改运行中的原审核算法。
5. 每张纹理使用原QA、原QA与中央116×116区域交集两种掩膜，各注入4种已知平移，
   并列记录原算法与候选结果。新增常量、8像素周期纹理、固定种子不相关纹理负控制。
   错误自信判定、原QA可靠结果丢失或负控制被判可靠均令该样例失败。
6. 再读实际源与掩膜、检查代码及输入锁后，封存calibration.json/output.lock.json。
   重跑复核源及掩膜，已有正式文件不得重写；源或封存内容变化拒绝复用。

此处通过只能证明人工已知平移及负控制上的候选标定；不能证明真实跨波段、
跨分辨率或绝对配准合格，也不会更改已有质量掩膜或授权像元融合。之后还需冻结
独立真实验证样例、比较实际匹配结果和失败原因，再决定是否扩大审核范围。


### B3 候选布局的实际标定结果（2026-09-13 23:12 UTC）

| 真实纹理＋人工平移范围 | 原角落可靠/总数 | 候选可靠/总数 | 候选最大误差 |
|---|---:|---:|---:|
| 高分40张，原QA | 160/160 | 160/160 | 0.025像素 |
| 吉林一号16张，原QA | 64/64 | 64/64 | 0.075像素 |
| 高分40张，中央116像素限制 | 157/160 | 157/160 | 0.05像素 |
| 吉林一号16张，中央116像素限制 | 0/64 | 61/64 | 0.10像素 |

“可靠”包含正确判断≤5米及正确判断超限，不是全部通过5米阈值。
候选共442/448个案例可靠、0个错误自信判定；6个不确定案例仍保留。
原QA的224个原算法结果与此前封存标定逐项完全一致；候选未丢失原QA可靠结果。
168个负控制全部拒绝。高分每变体至少7/8张、吉林一号每变体7/8张完成全部8案例，
满足原每变体至少4个成功位置门槛；不改变该门槛补齐不确定样例。

高分候选版本eb10e06a2b38c81e21a2，吉林一号7897984e5b08fbf6f34b；
calibration SHA256分别为635f91158e003c27ebdc6b3c3a8a0faf43e23d3f8e2e9d6de2820f04c96d1557、
8a5df5fe86f4761c1d4a43cfecd7767d4c3ce286e832f039580c2914c00b4e24。
两族实际源/QA只读重跑完成，8个正式文件SHA及修改时间全部不变。
完整证据位于报告目录diagnostics/adaptive_window_calibration，含标定、重跑对账、
四组互不重复的成功/不确定对照图与index.html。这里使用人工掩膜，不属于C1的
真实云标签96组验收图，也没有修改现有全量审核结果或原始影像。


本次候选布局改动：新增7项回归先失败后通过；工作树全套687通过（285.07秒），
干净待提交快照475通过（221.74秒），各有1个已有依赖弃用警告。
两份快照Black、Ruff、仓库门禁、无隔离构建均通过。干净代码树
2ea79e5ad999307dd02e18ec9aed8884e65e4239；全部新增源/测试与该树逐项比对后提交。
本改动不涉及NPU模型或全国网格修改，不触发训练发布门禁；真实数据均保留原划分。


## A2/B1 Landsat存储网格纠正与隔离重跑（2026-09-13）

**证据与问题**：旧扫描统一要求128×128和10米，S1/S2的48个月包通过；
停止时Landsat已有17包、1,049,171张被同一结构判断拒绝，尚未执行像素块解码。
3个月包的首/中/尾9张原始TIFF实际为6×43×43，像元大小约29.7674418605米，
覆盖原1280米全国网格。父包SHA及成员字节SHA已核实，不是凭显示效果猜测。
存储像元大小不等于重新声明卫星原生物理分辨率或波段身份。

**处理**：按产品固定存储几何合同：S2 10×128×128/10米、S1 2×128×128/10米、
Landsat6×43×43/(1280÷43)米；继续核验唯一全国位置、CRS、无旋转仿射变换及
1e-9米像元浮点容差。错误波段数、42或128像素的Landsat不接受。每TIFF保存实际
shape与元数据，仍标记radiometry_status=unverified和physical_contract_verified=false。
不重采样或覆盖源影像，不修改已有空间划分。

**执行入口**：通用四根目录及dense-root之外，使用
`--stage dense-integrity --dense-audit-version v2 --dense-product landsat_local`。
限定2020/2021的24个原月包、并行2包；版本及产品有独立锁和阶段记录。
输出位于报告目录dense_integrity/v2/landsat_local/，含新summary和逐月shards；
旧dense_integrity_shards与summary、48个S1/S2结果保留，禁止拿旧失败当新成功。

**已验证**：4个新增回归先失败后通过，含真实结构的合成栅格、错误形状/波段数、
24月范围与旧报告保护、CLI产品锁。连同原2测试共6项通过。9张实际源成员完整解码、
网格检查通过，0失败；证据位于diagnostics/landsat_grid_contract_v2/pilot_verification.json。
全24月运行已启动；只有正式逐月产物发布后计作相应月包完成，启动不代表全量完成。
物理波段顺序、缩放和偏移仍待来源证明，不能据此生成基础训练统计。


Landsat修复检查完成：工作树691测试通过（291.10秒），干净待提交快照479测试
通过（220.55秒），各1个已有依赖警告。两份快照的Black、Ruff、仓库门禁和无隔离
构建全部通过。干净代码树6b2eb6ede8180d53dec4b1eb47cc3578e94ff426。
原48个S1/S2产物未覆盖，17个Landsat失败包及1,049,171条结构拒绝原因已独立留证。
首2包新v2扫描逐文件解码已有实际进展，完整24月尚未发布完成。

同期锁定吉林一号12包完整QA：版本bb9a00e17db76c6e2f24、部分波段目录版本
23e0c8c6ff38f16f5a18，质量锁SHA
1c50e1192b40f1d520f6884b140ca3099a047bd50f2831480066c8c8189ed05f。
对应12包QA配准20,893张、训练划分统计67,239分支已启动独立完整处理；
训练统计版本243b9ba72daa3735edc5，QA审核目录快照5b7b0da0fbc0b57e70d3。
这次由协调器启动的QA完整结束，未再发生初始化停滞；先前两次停滞根因仍未证明。
高分原始无QA组内审核177,505张已对账：94,356通过、81,752不确定、1,397超限，
不能替代仍在运行的QA约束审核，也不表示绝对配准或融合通过。
