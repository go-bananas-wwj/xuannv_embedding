# V5 嵌入升级实施合同

## 本轮配准改进及执行边界（2026-09-14）

针对CFOG候选正确位移处有效支持不足的问题，固定模板由64改为56像素，并依据
参考图实际特征有效区域选择四个不重叠窗口。70%有效覆盖、相关性、窗口一致性
和5米标准不变；先重跑原56冻结位置的完整已知位移和负控制。此结果是标定，
通过后仍须排除全部251个已观察位置进行新的真实跨光谱检验，不能直接批准融合。
输入版本、算法、输出指纹、回归与对照图见数据细则的CFOG v2记录。

14包吉林一号QA已完整处理26,072场景、98,237分支，1,755场景明确QA缺失。
全部场景回执和质量表校验已独立复核；新快照原生审核、训练分支统计继续执行。
全64包、基础物理合同、绝对/跨分辨率配准、最终标签/索引/加载及完整验收材料
仍需完成，当前验收状态保持incomplete，未批准模型改造或训练。

### 01:14 UTC本轮结果

CFOG v2的448个已知位移案例446可靠正确、2不确定，168负例全拒绝。
吉林一号16位置全部通过；高分GF6一个位置丢失两个原QA可靠案例，整族按门禁
保留failed。中央区域改善不能抵消这项退化，不提高正式融合通过量。
全部实际源与QA缓存重读后，8正式文件SHA和修改时间不变。
Luna完成工作树707项、干净快照495项测试及两套格式、静态、门禁和构建检查，
全部通过；主代理核对具体标定结果和失败原因。代码及实际结果单独同步wwj。

吉林一号14包原生QA波段审核已完整发布24,321张：63通过、275超限、23,983
不确定；封存和完整观测清单已独立核对。新快照78,268训练分支统计、高分完整
缓存复核仍运行中；数据状态保持incomplete，详细版本和证据见数据细则。

## 已核实历史执行快照（2026-09-14 00:51 UTC）

验收仍 incomplete，未批准训练。吉林一号14/64包下载、解码、目录完成，新增
目录的QA/统计待处理；原12包QA、20,893张组内审核及67,239训练分支统计/缓存
复核完成。15/16包下载中。Landsat v2全部24月、1,480,709张完整检查并独立复核，0失败。高分177,505张原QA
组内配准审核全量完成，41,070通过、145超限、136,290不确定；284,230训练分支统计完整发布，10组合无失败；缓存复核运行中。

CFOG结构特征候选已接入CLI，9项回归、705项工作目录测试、493项提交快照测试
及格式/门禁/构建通过。冻结56位置448个位移案例标定完成，336可靠正确、112
不确定，168负例均拒绝；原QA的224个可靠案例无丢失，但中央区域未达覆盖门槛，
两族均insufficient_calibration，不替换正式配准。有效邻域诊断已锁定下一步需
调整的窗口尺寸关系；两族8个正式文件在完整缓存复核后不变。

OSM完成51,824/62,000位置，1位置4条投影失败已完成独立局部诊断，原失败记录
保留，尚未发布处置版本。基础物理合同、完整标签、最终索引、加载、验收材料
及后续吉林一号包处理仍未完成；完整范围与验收门槛保持原计划。

### 00:51 UTC全量产物补充

Landsat v2全部24月、1,480,709张解码完成。独立重算24个原ZIP的SHA及全部
观测表SHA，核对所有月份、网格位置、划分、43×43/六波段存储元数据，零失败。
旧17个失败月的1,049,171条成员源字节和划分全部不变。
证据：报告目录 `diagnostics/cfog_calibration/landsat_complete_verification.json`。
该结果只关闭存储网格/解码问题，不关闭物理波段与缩放合同缺证。

高分统计版本a02608e7b665228223ab已完整发布：284,230训练分支、70,780排除分支、
10产品/传感器组合、零统计失败。全部输入唯一性/训练划分、源SHA、各地区年份
波段贡献及所有正式输出SHA已独立核对；输出锁SHA为
b41316835a332c15d9e7dc08515bc1a83ec1793527a1237fa621baee78c2075f。
17个正式文件已保存SHA和修改时间基准，新的全量源/掩膜缓存复核已启动并实际
推进；不能把缓存复核启动写成完成。证据：
`diagnostics/cfog_calibration/gaofen_complete_statistics_verification.json` 和
`gaofen_statistics_before_replay.json`。仍是QA候选视图，normalization_authorized=false。

本轮CFOG代码/测试/文档已推送wwj并核对远端：
[d1019a7](https://github.com/go-bananas-wwj/xuannv_embedding/commit/d1019a70cc43d446115370e7179f275dbf4f6037)。
上述后续全量结果另作仅文档提交；未修改已验证源码，未重复无关测试。

## 阶段与授权

当前授权范围：下载、数据处理、验证和验收材料生成。不得运行嵌入模型训练、
更新其权重或自动宣布用户验收通过。已有云模型推理属于数据处理。
数据自动检查完成后状态最多为 `ready_for_review`；收到用户对具体数据版本的
明确验收后，才可以进入模型、训练阶段。质量或样本内容变化必须重新验收。

数据操作逐步合同见 [数据准备细则](data-preparation-v5.md)。原始影像、凭据、
完整清单、大型报告与权重均在外部数据盘；Git 仅保存程序、测试、配置和脱敏摘要。

## 完整执行顺序

| 编号 | 输入 | 操作 | 产物与检查 | 当前授权 |
|---|---|---|---|---|
| A0 | 原全国网格、旧质量版本、来源清单 | 固定源版本及校验值，保留原空间划分 | source.lock、输入审计 | 是 |
| A1 | 吉林一号64分包清单 | 两包先行，最多并发2，断点续传及SHA256 | 逐包下载状态 | 是 |
| A2 | 校验成功的包 | 安全解压、逐文件解码、数量对账 | 文件清单、异常清单 | 是 |
| A3 | TIFF与全国网格 | 几何匹配、同景分组、继承split | 文件/场景目录、覆盖表 | 是 |
| B1 | 原始产品元数据 | 核验波段及数值合同，统一原生读取 | 产品合同与数值审计 | 是 |
| B2 | 合格光学影像 | 云分类、30米缓冲、同景掩膜传递 | 原始分类、有效掩膜、质量表 | 是 |
| B3 | 同年清晰参考与高分 | 多窗口配准审核，不改写原始影像 | 合格/超限/不确定分类 | 是 |
| B4 | 年度及静态标签 | 检查网格、类别、年份、未知状态 | 标签manifest | 是 |
| B5 | 训练split合格观测 | 流式统计，每观测只计一次 | 分产品统计、贡献分布 | 是 |
| B6 | 观测及标签索引 | 季度基础观测、全年高分候选及默认选择 | 季度/年度索引 | 是 |
| B7 | 锁定新版本 | 各split100位置加载、缓存一致性、重复执行 | 检查结果、dataset.lock | 是 |
| C1 | 全部检查结果 | 完整报告及96组可视化，提交用户验收 | acceptance报告、审核状态 | 是；此处停止 |
| D1 | 已验收数据与旧8层权重 | 直接季度查询替代月度平均评测 | 锁定基线与缓存版本 | 验收后 |
| D2 | 特征和有效掩膜 | 逐像元归一化，高分无效时严格零增量 | 无效值不变性测试 | 验收后 |
| D3 | 每像元3个月64维特征 | 两层Pre-LN、8头、FFN64→256→64 | 时序单因素对照 | 验收后 |
| D4 | 同年GF/JL原生影像 | 分辨率编码→同景融合→年度聚合→季度门控 | GF/JL/联合对照 | 验收后 |
| D5 | 192通道精度主干 | 分别测试深度3×3、4×4→8×8空间token | 边界收益、计算开销 | 验收后 |
| E1 | 固定训练/验证子集 | 300位置≤5轮排障，3000位置每实验≤20轮 | 单因素后组合 | 验收后 |
| E2 | 优胜结构 | 同步数、同卡时比较8/12层，12层通过再16层 | 效果、速度、显存 | 验收后 |
| E3 | 基础影像+年度高分 | 先训练新增模块，再解冻末2层；细节头读64维 | 增强教师 | 验收后 |
| E4 | 冻结教师与基础影像 | 有效且时间状态明确区域进行蒸馏 | 全国基础学生 | 验收后 |
| E5 | 优胜配置、49600训练位置 | 有效批次64，最多45轮，验证选最佳 | 全国候选权重 | 验收后 |
| F1 | 锁定候选与独立标注 | 1/5/10-shot、空间留出、年份/缺失模态分组 | 发布候选报告 | 验收后 |

## 后续模型合同

基础编码64、精度192、空间768、时间384、最终64维。年度先验复用于四季度，
基础观测仅取查询季度，不对四季度强制一致。JL5米B1—B6、B0、10米B7—B12、
20米B13—B19分别原生编码后对齐；同名低分辨率重复波段不重复输入。

8层默认。12层相对优化后8层的少样本宏F1需增加至少1个百分点，按位置bootstrap
差值95%置信区间下界大于0，且任一主要任务退化不超过1个百分点，才进入全国阶段。
最终学生相对锁定基线提升至少2个百分点为目标，报告不确定性和资源代价。
现有架构8/12/16层分别为96,425,056 / 144,278,368 / 192,131,680参数；单位置
单季度STP主要前向约79.84 / 119.76 / 159.68 GFLOPs，新增模块另行计算。

## Git交付

每个可验证改动单独测试、commit、push至wwj并核对远端SHA。旧工作树已有未提交
修改需保留，不能捆绑无关模型改动。批次：文档；下载解压；目录；读取合同；
云质量；配准；标签统计年度索引；加载验证与验收。运行状态和完整文件清单保存在
数据盘，提交摘要须脱敏。每一步记录输入版本、参数、实际输出、处理/排除数量、
验证结果、Git提交与状态。任何未完成步骤必须明确标注，不以入口存在代替处理完成。

## 当前实施状态（2026-09-13）

最新核实快照（2026-09-13 20:19 UTC；完整目标仍未完成）：

| 数据部分 | 当前实际状态 | 尚待完成 |
|---|---|---|
| 全国网格 | 62,000位置及原划分锁定 | 最终数据锁对账 |
| 吉林一号 | 8/64包下载、解码及严格目录完成 | 其余包与全量处理 |
| 缺失波段 | 当前233文件已存在通道核实；逐波段QA已接入读取 | 全量QA、配准及最终视图 |
| 吉林一号逐波段QA | 固定6包11,192场景、42,244分支处理完成 | 当前8包及后续目录QA、视觉审核 |
| 高分云质量 | 177,505场景全量完成 | 视觉审核、绝对配准 |
| 基础影像 | 24/72包完整性完成 | 完整扫描、物理合同补证 |
| 年度静态标签 | 三产品各124,000项重建完成；DEM及依赖负例已修正 | OSM几何/类别来源继续核实 |
| OSM几何 | 全国已检查26,640位置 | 全量62,000位置及边界候选处理 |
| 统计、索引、加载、验收 | 核心有部分实现，完整产物未齐 | B5—C1全部完成后提交用户验收 |

数据、模型、训练、评测四部分目标均保留。本次仍止于数据交付验收。以下记录按时间
保留实施过程，较早“正在运行”的描述不是当前进程状态；实时证据位于数据盘报告。

### 历史实施记录

完整A—F计划继续保留；当前执行处于A/B数据处理，**未到最终数据验收**。
逐步骤输入、实际产物、实现与执行的区别、检查和GitHub提交，已补入
[data-preparation-v5.md](data-preparation-v5.md#实施记录2026-09-13进行中未提交最终验收)。

已锁定62,000位置和吉林一号64包发布版本，已启动下载、安全解压/像素核验、
高分全量云质量、标签数值和本地基础影像完整性检查。标签源文件的10项核验已完成。
同年度先验、流式统计和配准的核心实现有测试，尚不能据此声称对应全国数据产物已完成。

当前主要待解决项是基础影像物理合同来源缺失、完整下载/质量处理、真实配准审核、
正式统计/索引/加载验证，以及完整96组视觉材料。验收状态保持`incomplete`，
任何自动检查和后台数据作业均不代表用户验收。D—F模型、训练和正式评测没有启动。

15:30 UTC核实：高分177,505场景云处理及306项标签数值检查已经完成，但尚有视觉、
来源及年份合同审核。吉林一号第3、4包旧进程已经失败，已实现严格分块内部续传和
脱敏错误记录，准备保留原有校验分块恢复；不能把下载重启当作A阶段完成。

已补上年度OSM交叉审核实际入口`target-temporal`：同年历史来源、当前来源、未知状态、
置信度和覆盖值共同检查，逐块SHA绑定已完成数值审核。全量执行结果另在数据盘记录，
不能因为此入口及回归通过就把整个标签门禁标为完成。

吉林一号固定32场景已完成5m/10m冻结云推理对照。原生重跑与旧结果完全一致；10m候选
改变了大量掩膜，实际图例和数量已记录在数据细则。它目前只是质量诊断，缺少人工云
真值评分，不能据此直接替换正式QA或宣布数据质量达标。

15:58 UTC核对：下载恢复`8d303e1`、年度OSM交叉审核`53e7ca9`、云分辨率对照
`6b7554c`均已推送wwj。实际完整包2/64，基础完整性10/72，年度交叉审核22/68；
其余处理继续执行，细则中保留输入、产物、检查及远端提交对账。目标没有缩减为样例交付。

真实纹理配准标定发现旧循环相位匹配存在错误合格判定，已改为带掩膜的边缘归一化
互相关。同一批56张真实训练影像的224个已知平移试验全部通过，错误合格判定由13例
降为0；这验证的是测量算法，尚不能替代全量真实跨波段/跨传感器配准产物。

原生波段校准及全量审核已接入统一CLI，使用显式产品与版本参数；缓存绑定实际像素、
观测元数据和软件版本，重复校准不会无故改写有效锁。当前发布的原生审核版本为v5，
56个真实标定观测均固定；这属于B1/B3的数据质量检查，不授予像元级跨传感器融合资格。


2026-09-13 16:53 UTC执行核对：`86a5528`已push并验证远端SHA；原生波段审核已实际
处理首个128观测批次。GF输入177,505场景：首批121 passed、7 uncertain；JL输入当前
3包目录中的5,245个2020/2021 MS观测：首批6 passed、29 over_limit、93 uncertain。
这是按观测ID顺序读取的进度，**不能推算全国比例，也不能把光谱差异导致的匹配不确定
当作已证实错位**。原始影像保持不变，后续固定抽样做视觉复核。

新增有限续查机制：每批已校验包先更新目录，然后比较原生波段输入指纹、版本、标定
SHA、算法SHA和最终产物；当前目录未审核时运行band-alignment，已有同产品审核进程
存活时等待。只有完整目录的审核已结束后才进入原有全量云推理。超限/不确定仍保留为
异常状态，审核结束不等于像元级融合合格。失败的同一阶段/输入不会在轮询中无限重试。
续查显式传递`--alignment-version v5 --workers 2`；该父进程仅协调已授权数据阶段。
回归覆盖增量目录顺序、活跃任务避重、版本/源/算法变化、失败停止及不得进入训练。
12项相关测试、全仓Black/Ruff与内容门禁通过，构建通过后提交此改动。


原生波段诊断已实际生成GF/JL各12组固定图例，入口为`band-review`。图例保留原始
波段、叠加、匹配窗口与只用于显示的平移预览；部分JL边缘重影减轻是排查线索，
不是已验收的自动校正。云主导的GF例子继续待地面参考核验。597项全量测试通过；
这些材料补充B1/B3，不替代C1规定96组正式云质量/网格样例。当前源完整4/64包。


B4新增年度源到标签的实际重建核验：WorldCover、CLCD和夜光均从已锁定原ZIP按
全国网格重建，再比较标签值和有效掩膜。32位置×两年×三个产品的192项试验完全
一致；已接入`target-geometry`，完整62,000位置核验另行运行和记录，DEM/OSM几何
尚未由此覆盖。源SHA、像元SHA和源变化均纳入重复运行校验，不倒推缺失历史证据。
原生波段诊断代码`c226516`已push并核对远端SHA；全量数据验收状态仍为incomplete。


`d32b11a`已push并核对远端；只含该Git提交的独立副本391项测试及格式/门禁/构建通过，
完整工作树603项测试通过。三个年度标签各124,000项全量重建已实际推进，具体数量见
数据细则和实时报告。吉林一号仍为4/64完整包，B3绝对配准、DEM/OSM几何、基础物理
合同及B5—C1最终材料仍待完成，不能把已运行作业或当前零差异批次写为数据验收完成。


DEM/坡度重建已接入`dem-geometry`，从两段原始ZIP直接读取1,982个栅格的对应窗口。
32位置64项试验一致，并单独检查坡度求导是否涉及无效高程邻域；全量124,000项
尚需实际完成。年度夜光与CLCD各124,000项全量重建已完成且无差异；WorldCover
完成124,000项后发现5个接缝像元冲突，均落在已有无效标签区域，核验顺序正在修正，
原失败结果保留，尚不把该产品标记通过。


WorldCover v1的5项异常均为已有无效掩膜内的单像元接缝冲突。v2修正核验顺序，
在最终有效区域内仍严格拒绝任何源类别冲突；固定5例复核无图值或掩膜差异。
v2全量结果单独保存，原v1失败报告保留。DEM入口`bc3909d`已push，独立副本396项
测试通过；年度OSM68组交叉核验已完成，无矛盾，但OSM几何/负例来源门禁仍待完成。


输入锁新增实际网格副本复核：旧锁存在也须核对新数据目录网格SHA，拒绝目标端
划分被改或文件丢失。实际三方SHA核对通过，原62,000位置/划分未变。DEM和
WorldCover v2完整核验继续运行；`0fd1687`已push并核对远端SHA，未进入训练。


WorldCover v2已完成两年124,000项实际重建且无有效值/掩膜差异，5个原无效接缝
冲突单列保留。新增可靠负例证据核验`target-negative-rules`，实际首32位置256项
无差异；全量496,000项尚需执行。完整工作树615项测试通过，格式、门禁和构建通过。
DEM全量核验发现旧瓦片范围筛选遗漏边缘数据的真实差异，继续保留证据并处理；
规则可复现不等于标签准确，数据总验收仍为incomplete。


可靠负例原规则已完成全量496,000项且无矛盾，bd1ed1c已push并核对远端。
DEM真实差异新增独立修正层入口dem-corrections及共同读取，不覆盖历史标签；
必须完整源审核结束后实际生成全位置manifest和稀疏修正，坡度只保留完整有效求导
邻域。619项全仓测试通过；完整修正产物、依赖负例及数据总验收仍需继续完成。


DEM全量修正已实际覆盖62,000位置，11处补丁补回349个高程像元、修正353个有效
坡度值，并撤销640个缺少邻域支持的坡度有效标记。依赖负例同步重建11位置两年
22项，10项需要补丁：撤销640、新增80个水域负例，其他三类保持不变。原Zarr保留。
17位置34次实际年度读取通过，622项完整工作树及410项独立副本测试通过；
DEM修正23cbb0b已push。吉林一号下载已到6/64包，数据总验收仍为incomplete。


完整标签修正链已实际重跑，25个正式产物SHA全部一致，锁及补丁未重写。
依赖负例0ea9cd0已push并核对远端；当前只证明这部分数据修正及重复执行已完成，
完整A0—C1目标继续保持，最终验收及后续模型训练均未授权。


OSM新增osm-geometry入口，实际首32位置两年两分辨率128视图重建无差异；
索引及原始PBF/ZIP、历史日期、几何覆盖与四种标签字段共同核验。同时独立记录
道路/点在足迹外仍影响目标像元的边界候选，候选不自动替换监督。全国248,000视图
需要完整执行；试验通过不代替全量来源或标签精度审核。


缺失波段恢复已实际处理当前174个隔离文件；规范分支只读取具名目标通道，缺失
通道保持无效，合并目录69,502行通道状态明确。60文件目标补充波段完整，114文件
仍有目标通道缺失；2020/2021共71文件、65位置。QA/配准和最终视图资格另行核验，
未进入训练。完整工作树634项及独立副本422项测试通过，数据总验收仍为incomplete。

补充目录38b1822已同步GitHub。后续固定相同输入重跑的正式锁，并记录运行库
版本；当前6包实际重跑的5个正式文件SHA一致，数据资格仍待QA与配准审核。

后续分包续处理加入缺失波段补充目录的版本/输出核验，避免仅处理首6包。637项
工作树测试及425项独立副本检查通过；边界标签另形成7组候选诊断，正式标签保持。

逐波段吉林一号QA新增`jilin-quality`：缺失非云输入通道不清零其他通道，缺B5/B4/B6
明确QA缺失。32真实场景128分支通过读回和缓存重跑；完整目录QA继续执行，云
检测准确率与最终数据验收尚未通过。实现与验证细则见数据准备文档。

新版QA按每批完整目录自动续处理，32场景试验不会被认作整批完成。现已启动固定
6包目录11,192场景的逐波段QA；流程登记设备任务身份避免重复推理，当前尚未完成。

原生波段审核新增进程执行入口，固定40真实高分影像中8进程相对2线程约快2.36倍，
结果逐项一致。保留原v5算法、校准和已完成缓存，完整质量审核范围保持不变。

高分全量审核已切换8进程并复用30,203份已完成结果；吉林一号后续批次采用同一
并行入口。原算法与校准保持，验收仍需完整观测审核和绝对配准证据。

8包吉林一号QA出现内存映射阻塞后，已保存诊断和失败记录，确认异常进程退出，
保留6包正式质量锁。在设备6完成32场景恢复验证（25缓存、7新推理），128分支
公共读取及576个缓存数组摘要核验通过，随后恢复14,902场景完整QA并登记任务身份。
模型及质量规则未变，准确故障根因仍待确认。恢复执行不代表完整数据交付；
A0—C1目标及用户验收边界保持，统计、索引、加载和完整验收材料继续推进。

当前8包逐波段QA已完整处理14,902场景、56,236分支，其中980场景明确缺少QA。
新增清晰地物校准入口，实际用原40张高分和16张吉林一号固定训练样例比较掩膜
前后配准，并做224个已知平移测试。两族均因部分变体的清晰样例不足而未通过
校准门禁；高分17个原始通过样例在排除云后无法可靠判断。保持门槛和原始记录，
后续单独冻结按QA覆盖选择的校准集合，不能将本次试验当作全量或绝对配准通过。

晴空训练校准入口现已实际运行：每变体2020/2021各4位置，选择仅依赖QA覆盖、
原划分与固定哈希顺序，清单在测量前锁定。56张真实影像的224个已知平移案例全部
通过原门槛，高分/吉林一号最大误差分别0.025/0.075像素。实际波段间仍有大量
无法可靠判断及2个超限观测，必须单独审核；方法校准通过不代表全量数据配准通过。

已补齐`clear-band-audit`入口：先重验通过的晴空方法校准，再冻结实际QA目录，
对全部原生多光谱观测逐波段测量；全云和缺波段均显式登记。校准选择阈值不会用于
全量审核筛除困难影像，缓存复用重新检查实际源文件与掩膜。高分和吉林一号各32张
真实试运行无读取拒绝，分别12/1张组内通过、20/31张无法可靠判断；不表示绝对配准
或数据验收通过。完整处理及跨分辨率审核继续执行，训练边界保持不变。

完整QA约束组内审核已同步GitHub提交210149d并实际启动：高分177,505张；吉林一号
使用新完成10包质量快照，共17,414张原生MS。10包云处理完整覆盖18,647场景、
70,329分支，其中1,237场景明确QA缺失。完成10包处理不等于64包完成或用户验收，
同年先验、训练集统计、最终加载和完整验收材料继续受原计划边界约束。

高分辨率训练统计现已接入统一入口：六类原生分支采用共享质量读取，按产品及
传感器变体流式累计，明确排除非训练划分、记录重复内容及地区/年份贡献，空通道
失败。高分与吉林一号各128条真实试运行完成；完整训练候选分别284,230和55,977条。
统计候选不会自动批准训练使用，后续训练视图改变掩膜或筛选时必须生成对应新版本。

新增逐窗口失败诊断已在80张冻结训练样例上实际运行，并与原配准逐对重放一致。
其中5张的25对波段存在角落无可用窗口而内部覆盖合格的现象；另有相关性不足的
独立问题。后续窗口布局或跨光谱特征改进需分别锁定假设、做已知平移校准和真实
样例对照，当前不改变配准状态。7组诊断图已留存，不能代替完整96组验收材料。

吉林一号10包55,977训练分支统计的完整源/QA缓存复核现已结束，15正式文件的
SHA与修改时间保持不变；高分统计及其余数据门禁继续执行，验收边界保持。


### B3 窗口改进的验证边界（2026-09-13）

基于此前固定角落漏采有效区域的证据，新增参考波段独立选择、不重叠的候选窗口
布局与独立CLI标定入口。原相关性、歧义、一致性和5米阈值保持不变；先以原56张
冻结训练纹理及已知平移、受限中央有效区域和负控制验证。算法与原审核并存，
实际完成数量及结果写入数据细则；此项不自动提升全量观测的配准状态。


候选布局已完成56张真实纹理的448个已知平移案例：442个可靠、0个错误自信，
6个仍不确定；168个负控制全部拒绝。吉林一号中央受限区域由0/64可测变为61/64，
原QA的224个可靠结果保留。8个正式文件完整只读重跑不变。下一步为独立真实
跨波段验证，不能把人工平移标定收益写成真实配准合格率提高。


### A2/B1 分产品存储网格纠正（2026-09-13）

查明旧完整性检查错误地对Landsat要求128×128/10米；实际源为6×43×43及1280/43米，
原全国地理范围不变。已新增分产品结构检查及v2隔离重跑入口，6项测试和9张实际源
检查通过，完整24月重跑进行中。旧48个S1/S2结果及17个Landsat失败包记录保留。
该修复只确认存储几何，不把尚未核实的波段顺序或物理缩放设成默认值。


Landsat分产品几何修复通过工作树691测试、干净快照479测试及两套格式、静态、
仓库门禁和构建检查；完整重跑的实际进度与所有未完成项见数据细则最新快照。


### B3 候选真实影像的进一步检验（2026-09-13）

新增独立位置的真实波段对照入口，排除此前两族原始/清晰标定及80诊断样例位置，
按年份、传感器和有效比例固定选样，先锁定再测量。原结果精确重放后，候选须经
反向平移闭合及三种已知扰动检验；全部自洽只形成待复核证据，不替代真实对应点
标注或原正式配准结论。完整源、QA、代码和封存复核方式详见数据细则。


真实候选检验已封存80位置400对：84对全部自洽、13对正向可靠但未完成全部检查、
303对正向不确定。新增29对自洽局部结果不等于新增整景可融合；仅1个位置全部波段
自洽且≤5米，且原本已经合格。5组真实对照图、来源锁及完整重复运行证据齐备，
原数据与正式QA/配准结论保持原版本。此项通过696工作树测试及484干净快照测试，
相关格式、静态、仓库门禁和构建检查均通过，完整结果见数据细则。


原始文献核查支持下一步比较结构表示，而不是预设继续增加窗口必然有效：
[CFOG](https://arxiv.org/abs/1808.06194v8)与[RIFT](https://arxiv.org/abs/1804.09493v1)
提供跨模态辐射变化下的结构匹配方法。这只是待验证假设，尚未实现；新方法必须
重新标定、保留真实有效掩膜，并使用排除已看过位置的新独立检验集合。
