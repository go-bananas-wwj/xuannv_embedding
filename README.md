# XuanNv Embedding

XuanNv Embedding is a region-agnostic implementation of monthly geospatial embeddings for
multi-source Earth observation. It produces a 64-channel, 128 × 128 dense embedding map for
each 1,280 m × 1,280 m patch.

The production repository is being rebuilt from an audited whitelist. Model weights, source
imagery, embeddings, checkpoints, and other large artifacts are distributed separately and are
never committed to this repository.

## Status

The `v1.0.0` production migration is in progress. Until the release tag is published, interfaces
on the development branch may change.

## License

Licensed under the Apache License, Version 2.0. See `LICENSE` and `NOTICE`.
